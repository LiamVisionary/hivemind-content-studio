# Universal TTS voice direction template

Voice: {{voice}}
Provider: universal_tts

Direction:
{{performance_direction}}

Line:
"{{line}}"

Use written performance direction, not just pasted dialogue. The configured runtime is Universal TTS (`/v1/audio/speech`), not direct provider sidecar ports.
