# Changelog

## 2026-09-06 20:36:03 +08:00 — The studio publishes the media-model catalog HivemindOS reads for its /image-gen and /video-gen picker

- Status: Committed locally on `publish/media-model-catalog-2026-09-07` (not pushed); the studio's targeted suites (65) and the 209-test regression passed in the main tree, ruff clean on every touched file (the one F401 it reports, `RemoteAccessError` in `control_api.main()`, is at HEAD and outside this change).
- User-facing result: HivemindOS chat's new runtime-and-model picker lists this studio's image and video models beside the fleet's and the hosted ones — the local ComfyUI and Media Studio workflows as ready rows that run from chat through the Media Studio MCP, the provider-account rows (OpenAI, xAI, Higgsfield, MUAPI) with where they bill and a reason ("Runs only inside Hivemind Content Studio for now."), the hosted place left for HivemindOS to price — and still lists them, marked unavailable, when the studio is closed. The studio's own document (the route, the MCP tool) lists HivemindOS's rows in return — its fleet, its priced hosted models — from HivemindOS's last snapshot, deduplicated against the studio's own lanes.
- Mechanism: `media_models.py` (new) projects `media_catalog()` into the shared `media-model-catalog` v1 document (`catalog/media-model-catalog.v1.schema.json`, byte-identical to HivemindOS's copy): one source per provider with readiness, `needs` and credential NAMES; one row per pickable model with place/cost/credential from a table mirroring the frontend's `PROVIDER_TRANSPORTS`, `execute` (`media-studio-mcp` + `workflowId`/`backend` for comfyui and media-studio-mcp rows, `hosted-media`/`automatic` for the hosted row, `none` + `openUrl` for everything else), `ready` only for a reachable local workflow, and a prose `reason` that never names a variable (credential-name and address sentences fall back to "Needs a credential in the studio's Providers view"). `workflow-default`, the stickman/static renderers and routing-only graphs are left out. `MediaModel.requires_image` (new field, read off the live registry's `requires.image`) surfaces as `capabilities.requiresImage` on workflow rows. `media_model_catalog()` then merges `~/.hivemindos/media-catalog/hivemindos.json` (`merge_peer`): rows rebuilt from known fields only, deduplicated by key and — for this-machine rows — by lane (kind + workflow/model id, whatever provider name each app uses), pointed at one `hivemindos` source ("Listed from HivemindOS's last snapshot."); HivemindOS's priced hosted rows are `ready` here when the studio's own hosted readiness saw the app answer (the hosted client runs a specific model id), and every merged row is unavailable/not ready with "HivemindOS is not running." (or "…not linked to this studio.") when the snapshot is the only evidence. The published vocabulary is the shared one: placeLabel "This machine", cost "Free · stays on this machine" (the studio's own UI keeps "This Mac"). `api/media_models.py` (new) serves `GET /api/media-models?kind=` as `{"ok": true, "catalog": doc}` on the machine lane as a PROJECTION of the simple catalog's cached media inventory (`ctx.simple_catalog_cache`, shared refresh thread, re-projected once per sweep) — no readiness sweep, no outbound call, for a machine-lane read; before the first sweep lands it answers `{"ok": true, "pending": true, "catalog": <empty valid doc>}` with `Retry-After: 2` like `/api/simple/catalog` — and writes the snapshot to `$HIVE_HOME/media-catalog/hivemind-content-studio.json` (else `~/.hivemindos/…`; directory 0700, file 0600, temp file + rename) after every fresh projection. MCP: tool `list_media_models(kind="")` and resource `studio://media-models`.
- Privacy: the document is a projection of the inventory `/api/catalog` and `/api/providers` already serve without a session; a machine-lane read starts no probe and makes no outbound call. It carries no prompts, media names, absolute paths, Tailnet addresses or credential values. The image route and the dashboard-server discovery proposed in the design were deliberately NOT changed: `/api/media-studio/image` stays owner-only and the vault stays a recipient of every still.
- Files: `src/hivemind_content_studio/media_models.py` (new), `src/hivemind_content_studio/api/media_models.py` (new), `api/catalog.py` (exposes its cache, refresh kick and TTL on `ctx`), `media_catalog.py` (+`requires_image`), `control_api.py` (module registration; sixth machine-lane read), `mcp_server.py`, `docs/MEDIA_MODEL_CATALOG.md` (new), `docs/README.md`; tests `test/studio/test_media_models.py` (new; the "real inventory" test pins the registry, the readiness probes and the key variables so it projects the built-in matrix deterministically), `test_route_gates.py` (MACHINE_LANE is six, justified in the comment; `_locked_client` points HIVE_HOME at the test directory so the sweep never writes the real snapshot), `test_mcp_agent_contract.py`.
- Verification: `PYTHONPATH=$PWD/src .venv/bin/python -m pytest -q test/studio/test_media_models.py test/studio/test_route_gates.py test/studio/test_mcp_agent_contract.py test/studio/test_hivemindos_hosted_media.py test/studio/test_text_models.py test/studio/test_control_api_polish.py test/studio/test_image_router.py test/studio/test_media_studio.py` and the whole `test/studio` suite — counts in the session summary.
- Intended commit message: feat(catalog): publish the shared media-model catalog HivemindOS reads for its /image-gen and /video-gen picker

## 2026-09-02 21:58:53 +08:00 — A drawn character sheet is shown from the bytes in hand, and a sheet that cannot be shown says why

- Status: Uncommitted
- User-facing result: Liam drew The Gardener's character sheet in the Story studio and the card showed a broken image with the alt text "The Gardener reference sheet" in it. Mechanism: `draw()` promotes the generated picture to a reference (`POST /api/media-studio/references`), which the server seals to the OWNER VAULT (`_vault_public_key()` → `seal_private_media_e2e`) and serves back as an E2E envelope; the card then re-fetched that URL and asked the tab to decrypt it, and `resolveMediaSrc` is fail-open — with no usable vault key in the tab it hands back the envelope URL, so the `<img>` pointed at ciphertext. The Story studio's `Plate` had no locked/undecryptable/missing state, unlike History and the video cards (`VaultLockedTile`). Two fixes: (1) the picture is read ONCE in the clear (`mediaSourceToDataUrl`), handed to the upload, and primed into the media cache for the new reference URL (`e2eMedia.primeResolvedMedia`) — a freshly drawn sheet renders from the bytes already in memory, no second fetch, no decrypt, no dependence on the vault state; (2) `Plate` now consults `useMediaSealFailure` and renders `VaultLockedTile` ("Vault locked · click to unlock" / "Can't decrypt · sealed for a different key") inside the slot's own box, and an `<img>` `onError` renders "Missing · Redraw it" for a reference the server no longer has. The Redraw button is already beside every slot.
- Files: `packages/open-generative-ai/src/lib/e2eMedia.js` (`primeResolvedMedia`), `src/lib/outputToReference.js` (`dataUrl` option), `src/studios/StoryStudio.jsx` (`draw`), `src/studios/story/parts.jsx` (`Plate`), tests `tests/e2eMedia.test.js` (+1), `tests/storyPlate.test.js` (new).
- Verification: `node --test tests/storyPlate.test.js tests/e2eMedia.test.js` 6 passed; full JS suite 1113 passed, 1 pre-existing failure (video-tag scan over untracked inpaint work). Live on the fixture harness (vite 5573 → node fixture 8999): loaded the worked example, set one character's `sheetUrl` to a 404 and another's to an `X-E2E-Media` envelope through the card's React props — "Missing / Redraw it" and "Can't decrypt / Sealed for a different key" both rendered inside the 74×154 slot (the first cut had the shared tile bursting to full card width; fixed by letting the slot box own the shape). `vite:build` done; 8765 serves `dist` fresh from disk, so this is live without a restart. Not reproduced: which of the three states Liam's tab was in — the studio keeps no access log and the story state is encrypted in the browser; the freshly-drawn path no longer depends on it.
- Intended commit message: fix(story): show a drawn sheet from the bytes in hand, and name a sheet that cannot be shown

## 2026-09-02 21:49:15 +08:00 — The ChatGPT sign-in callback answers on both loopback families, and the studio finds the app run from source

- Status: Uncommitted (studio) · Uncommitted in `~/hivemindos` main (`src/lib/services/openai-oauth.ts`)
- User-facing result: Pressing "Sign in" on the expired ChatGPT account in the producer picker stopped at "The sign-in would come back to localhost:1455, which resolves to ::1 on this machine — and the listener is bound to 127.0.0.1 only, so the callback is refused." That sentence is the studio's own preflight (`hivemindos_oauth.callback_reachability`) refusing to open the browser for an approval that would have died at the callback, and it was accurate: the HivemindOS app binds the OAuth callback server on demand at `openai-oauth.ts:260` with `server.listen(1455, "127.0.0.1")`, while the redirect registered with OpenAI is `http://localhost:1455/auth/callback` and macOS resolves `localhost` to `::1` first (a refusal is definitive; browsers do not fall back). Fixed in HivemindOS: one listener per loopback address (`127.0.0.1` and `::1`, never `::`, which would expose the callback to the LAN), both closed together, the second released if the first refuses, and a machine with no IPv6 loopback tolerated. Proved live on the dev app: `start_oauth_login("openai")` from the studio now shows `node … 127.0.0.1:1455` AND `[::1]:1455` in lsof, `curl localhost:1455/auth/callback` answers 400 (alive), and the preflight reports reachable. Separately, "the desktop app is open" while the studio said "direct": the app on this machine is the DEV build (`scripts/tauri-next-dev.mjs`, `tauri.conf.json` devUrl on 5021); the studio's models route probed only the packaged port 5020. It now tries 5020 then 5021 (a configured `HIVEMINDOS_URL` alone), remembers the address that answered, and the live status call came back `route app`, 399 models with the house tiers priced in credits (Auto ≈ 15 credits per draft, Fast ≈ 4, Deep ≈ 88), balance "999 credits" through the app.
- Files: `~/hivemindos/src/lib/services/openai-oauth.ts` (servers[], `listenOn`, `OAUTH_LISTEN_HOSTS`; eslint + tsc clean), `src/hivemind_content_studio/hivemindos_models.py` (`DEV_APP_URL`, `candidate_urls`, `base_url` from the last probe, `app_is_running` over candidates), `test/studio/test_text_models.py` (`test_an_app_run_from_source_is_found_on_the_dev_port`).
- Verification: `pytest test/studio/test_text_models.py test/studio/test_hivemindos_hosted_media.py` 38 passed; the live probes above; `zimage-stack restart` run with nothing rendering and no local model loaded.
- Intended commit message: fix(models): find the app on its dev port; (hivemindos) bind the OAuth callback on both loopback families

## 2026-09-02 21:13:42 +08:00 — The producer picker could not pick, and now it is one priced list

- Status: Uncommitted
- User-facing result: Pressing any model in the Story producer's "Who writes for you" picker did nothing — Liam hit it on the free Swarm Sovereign Scout row, but every row was dead: `ModelSourcePicker.jsx` called `rememberModelUse()` without importing it, so each click threw a ReferenceError before `onPick` ran (committed 2026-08-26 in 46890a4, so the built studio has been like this since). Fixed, and guarded by a test that checks every name the picker imports from `textModels.js` is exported there. The picker itself was redone: the four tabs (On this machine / HivemindOS / Cloud models / Your accounts — two of which were the same bill) are now ONE list in three sections, each section a bill (this machine's RAM, HivemindOS credits, the owner's own accounts), with an "All" view that leads each section with what it is wanted for (the default, the free model, loaded local models, used-before rows) and says how many more a search or "show all" reaches; filter chips narrow to a section; one search box reaches all 1,002 models. Every row carries a right-aligned price column: "Free · private", "Free · answers up to 1,024 tokens" (the free tier's cap, stated before a draft of eight concepts hits it), "≈ 4 credits per draft" on paid HivemindOS rows, "≈ $0.03 per draft" on provider-account rows (the exact per-1M rate is the tooltip). The collapsed bar carries the price too ("OpenAI: GPT-5.6 Luna · HivemindOS · ≈ 4 credits per draft"). Earlier today: the direct-gateway route sent no `Idempotency-Key`, which the gateway has refused since 2026-08-25 as "This app version cannot safely retry a paid request. Update HivemindOS" — fixed with a fresh key per press.
- Root cause of the estimate's numbers: the gateway quotes retail USD per token and HivemindOS values a credit at 500 per retail dollar (`HIVEMINDOS_CREDITS_PER_RETAIL_USD`); the studio now mirrors that constant (`hivemindos_models.CREDITS_PER_USD`) and puts `promptCreditsPerMTok` / `completionCreditsPerMTok` on every HivemindOS row (app route: passed through from the app's own metadata; direct route: computed) and `promptUsdPerMTok` / `completionUsdPerMTok` on provider-account rows. One press is sized from a measurement: a "concepts" ask is ~360 prompt tokens before locked context, so a Story draft is estimated at 1,000 in / 2,500 out (`DRAFT_USAGE`); the prompt helper at 1,200 / 700 (`PROMPT_USAGE`). Task budgets (4,000–6,000) are ceilings, not costs, and are not used.
- Areas changed: `StoryStudio.jsx` dropped 157 lines of picker plumbing (connect / link / save-key / remedies) that duplicated `useModelSources`, which now takes `onOpen` so a repair that switches sections also opens the popover; `PromptHelperDialog` passes `PROMPT_USAGE` and lets the picker default to the All view; the credit balance label rounds ("999 credits", not "999.385").
- Files: `packages/open-generative-ai/src/components/ModelSourcePicker.jsx`, `src/lib/textModels.js` (SECTIONS, featuredRows, costLine/creditsPerCall/usdPerCall/rateLine/detailLine, DRAFT_USAGE/PROMPT_USAGE), `src/lib/useModelSources.js`, `src/studios/StoryStudio.jsx`, `src/studios/story/ProducerBar.jsx`, `src/dialogs/PromptHelperDialog.jsx`, `src/hivemind_content_studio/hivemindos_models.py` (credits_per_mtok, row rates, Idempotency-Key, label rounding), `src/hivemind_content_studio/provider_models.py` (_usd_per_mtok), tests in `tests/textModels.test.js`, `test/studio/test_text_models.py`, `test/studio/test_provider_models.py`.
- Verification: `node --test tests/textModels.test.js` 26 passed; full JS suite 1110 passed, 1 failed (`no video player offers the browser its own download path` — a repo-wide `<video controls>` scan tripping on untracked video-inpaint work, not this change); `pytest -k "text_models or provider_models or story or hivemindos_models or prompt_helper"` 138 passed. Live in the Browser pane on the vite fixture harness (real `/api/text-models` catalog: 1,002 models, direct route, credits configured): clicking the free row sets `aria-checked` and the bar reads "Swarm Sovereign Scout · HivemindOS · Free · answers up to 1,024 tokens"; searching "claude" lists Fable 5.1 at ≈ 176 credits per draft, Sonnet 5 at ≈ 35; the accounts section shows 6 of 9 connected with the expired ChatGPT sign-in and its Sign in button. `vite:build` done and `zimage-stack restart` run, so 8765 serves the new bundle and the priced catalog. Gateway fix proved with one live paid completion ("ok") and the refusal reproduced by stripping the header.
- Intended commit message: fix(story): the producer picker can pick again, and every row says what a draft costs

## 2026-09-01 16:40:00 +0800 — The DENIED spam in the machine's access ledger was this suite, not the studio

- Status: Uncommitted
- User-facing result: `passbook access` on this machine showed `hivemind-content-studio` reading `HIVEMINDOS_DASHBOARD_DEVICE_TOKEN` in bursts, every one DENIED, which reads exactly like the studio polling for a credential it has no grant for. It is not. **The running studio never asks PassBook for that key at all** — `build_control_app()` calls `apply_shared_hive_env()` at boot, so the token is already in `os.environ` and `_dashboard_token()` returns on its first branch; 24 polls of `/api/providers` over two minutes produced zero ledger rows, and the hosted-media provider reports "route did not answer" rather than "token is missing", which is the proof it held a token. **The rows were written by `pytest`.** `passbook.set_recorder()` is process-global and monkeypatch cannot undo it; `build_control_app()` arms it via `enable_access_stamps()`; and test_gpu_rentals_api's `test_account_state_reports_burn_and_runway` calls `monkeypatch.undo()` — dropping every patch it holds, the autouse `HIVE_ENV_FILES` redirect that keeps the suite off the real store included — and then builds an app. From that test on, the machine's real hash-chained ledger was armed for the rest of the run, and because the redirect points every later read at a store that does not exist, each came back empty and was stamped as a DENIED read by this app. One run left 45 rows, 37 of them for that one key, in bursts as pytest walked the files — the observed signature, at the observed cadence. A full run now adds **0**.
- Fixes, in order of what actually stopped it: (1) `test/studio/conftest.py` gains `_no_test_stamps_the_machines_access_ledger`, an autouse fixture that clears the recorder before and after every test, so none inherits an armed recorder and none leaves one behind — the same class of process-global leak `_restore_process_env` already exists for. (2) `enable_access_stamps()` now refuses to arm the machine's ledger when `HIVE_ENV_FILES` says this process was pointed away from the machine store — the guard `join_hive_env()` already applies, for the same reason, and it covers real redirected instances (a sandbox, a second instance), not only tests. (3) Defence in depth, not the observed cause: `request_credential` — the door every credential read in the studio goes through — now remembers a refusal per key and answers from memory until a backoff window expires (30s, doubling to a 5-minute ceiling, cleared by the first answered ask). Grants are never cached, so a rotated value is picked up on the very next ask and granted reads keep leaving their receipts; a sealed value counts as a refusal; redirected processes skip the memory entirely. This matters on a machine where the studio genuinely is not granted the key, where every `_dashboard_token()` call would otherwise reach PassBook on every poll.
- Files: `test/studio/conftest.py` (`_no_test_stamps_the_machines_access_ledger`, `_forget_credential_refusals`), `src/hivemind_content_studio/shared_env.py` (`HIVE_ENV_FILES` guard in `enable_access_stamps`, refusal memory in `request_credential`), `test/studio/test_broker_integration.py` (a redirected process never stamps the machine's ledger; a real one still does), `test/studio/test_shared_env.py` (one ask per window with doubling and cap, a grant clears the memory and grants are never cached, sealed counts as refused, redirected skips the memory).
- Verification: full studio suite `1146 passed, 16 subtests passed`. Measured against the machine's own ledger across three full runs — 45 rows added (37 for the token) before the fix, 45 after the `enable_access_stamps` guard alone (which `monkeypatch.undo()` defeats), **0** after the conftest fixture. `passbook access --verify` still reports every row chaining onto the one before it; the stack was restarted with `zimage-stack restart` and `/api/providers` answers 200. No credential value was read or printed at any point — presence and DENIED/granted status only.
- Intended commit message: fix(tests): stop the suite stamping the machine's real credential ledger



## 2026-08-24 18:40:00 +0800 — A Story studio: the whole character-film production, one decision at a time

- Status: Uncommitted
- User-facing result: A new **Story** studio (nav → Studios → Story, `?page=story`) that walks one character-led short from an idea to a shippable clip in six gated stages. **Concept**: describe a human, a companion, a feeling and a world; a local model drafts 3–12 genuinely different directions with a hook, a friction, a reward and a signature detail each; shortlist two or three, compare them on five named axes, draw a contact sheet, then lock one — which writes the *character contract* ("When ___ happens, ___ tries to ___, while ___ responds by ___ — turning it into ___") and the per-character identity locks every later stage quotes. **Characters**: one editable sheet per recurring character (silhouette → face → pattern → signature → behaviour, ranked by how badly each shows when it drifts), drawn as a front/side/back reference sheet and promoted straight into the reference library, with an eight-point audit beside the result. **Location**: one *empty* 9:16 plate, with the things that can move picked while looking at the place. **Storyboard**: 4 panels / 16 panels / 2 precision frames, with the density recommended from the beat count and a squint test that reports four panels sharing one shot, a repeated action or a camera with no reason. **Motion**: a timed script — a named force, a response at each depth, one dominant verb per beat, a motivated camera and diegetic audio — plus warnings for a beat past the end of the clip, unscripted time, three actions crammed into one beat and a world where fewer than three depths move; a character-budget report that names the phrases costing space and saying nothing, a one-press cut for those, and a model-driven compressor for the rest. "Open in the Video studio" hands the script to the real composer, where the sheets and the plate are already sitting in the reference picker. **Gate**: nine checks in the order they are cheap to fix, each failure offering a repair that names ONE stage to change; a two-generation split for anything longer than one clip; a seven-beat caption builder that reports a second call to action; and the signal-to-next-move table. The whole production is encrypted in the browser and survives a reload.
- Root cause: not a bug — a purchased playbook (The Viral Character Method, Yume no Sekai) that the studio had no home for. Its decision system is a production *sequence*, and the app had every generator it needs and nowhere to hold the sequence: the Video studio owns one clip, the Image studio owns one frame, and the choices that actually determine whether a clip works — which pair, what they are locked to, where it is, how many beats, what moves — were happening in a text file.
- Fix: `src/studios/StoryStudio.jsx` plus six pure modules under `src/studios/story/` (`concept`, `characterSheet`, `location`, `board`, `motionScript`, `qa`, and a worked `example`), all testable under `node --test` because none of them import React. The producer is the SAME local llama-server the prompt helper loads, reached through a new `src/lib/localProducer.js` (runtime → load → wait → ask) and a new `/api/story/producer` route backed by `src/hivemind_content_studio/story_producer.py`: seven declared tasks, each with an instruction, a JSON shape shown verbatim, and a checker that refuses a concept with no signature detail, a contract with a blank part, a character with no never-change list, a location with nothing that can move, a board whose panels share one action, or beats with no named force — with ONE pointed retry that tells the model what was wrong rather than asking again blindly. Three new capability-matrix features (`story_character_sheet`, `story_location`, `story_board`) rate the image catalog per stage rather than once, because a character finetune draws a good turnaround and a bad empty street; the matrix's unmatched line stopped claiming to be about sprites. The sheet, plate and board go through `promoteOutputToReference`, so nothing has to be exported to be used as a reference. `ModelOption`/`RATING_TONE` were lifted out of `SpriteStudio` into a shared `studios/ModelFitPicker.jsx` rather than copied. The whole production persists through `composerState`'s `story` section — encrypted in this browser, opaque to the server. Two things found while verifying and fixed: the world-breathes sentence fitted every layer into "<force> moves ___", which produced "cold air moves the work coat shifts when the arm moves" as soon as the producer wrote clauses instead of noun phrases (it now hangs the responses off the force with a colon, grammatical either way), and the beat time boxes were 40px wide with 24px of padding, so "10" rendered as a clipped "1".
- **A sign-in is no longer started if it cannot come back.** Reported: pressing Reconnect opened the authorization page, the grant was approved, and the callback landed on `ERR_CONNECTION_REFUSED` at `localhost:1455/auth/callback` — the approval spent for nothing. Diagnosed rather than guessed: HivemindOS's Next server serves that path on **127.0.0.1:1455, IPv4 only**, the registered `redirect_uri` says `localhost`, and macOS `getaddrinfo` returns **::1 first** regardless of `/etc/hosts` order — and a refused connection is immediate, so nothing falls back (`curl 127.0.0.1:1455/auth/callback` → 400, `curl localhost:1455/...` → refused, exactly what the browser saw). The redirect_uri is registered with the provider and must not be rewritten, so `hivemindos_oauth.callback_reachability()` REPORTS instead: it parses the redirect out of the authorize URL and, for a loopback host only, probes it the way a browser does — the first getaddrinfo candidate and no more. That distinction is the whole check: Python's own loop tries every address and succeeds if any answers, which called `localhost:1455` reachable while Chrome could not reach it. `/api/oauth/{provider}/start` now answers **409** with the mechanism and the remedy, and the studio does not open the browser at all. A non-loopback redirect is never probed, because a false negative there would block a sign-in that would have worked. **The actual fix belongs in HivemindOS**: bind its server dual-stack (`-H ::`) so `localhost:1455` answers over both families.
- **The MUAPI key moved off the browser onto this machine.** It was the one provider still authenticated from `localStorage`, so a user whose HivemindOS already held `MUAPI_API_KEY` was prompted for a key the machine had all along — the same avoidable demand as the OAuth error below. New `src/hivemind_content_studio/muapi_proxy.py` behind an owner-only `POST|GET /api/muapi/{path}` forwards the studio's existing MUAPI calls with the key attached server-side from the shared Hive environment, plus `GET /api/muapi/status` reporting presence only (never the value). Deliberately a PROXY and not a re-implementation: the browser client already owns endpoint resolution, the poll cadence, cancellation, MUAPI's `{detail:{status:'failed'}}` envelope at HTTP 200, and the request-id contract a reload resumes from — re-expressing that server-side would be a second copy to keep in step, and the failure mode of the two drifting is a job the browser polls forever. So `muapi.js` gained one thing, `route()`: resolved once per page, it returns either `/api/muapi` with no auth header (this machine has the key) or the original host with the browser's key (standalone, or no server key). Everything else in that client is untouched. The proxy is narrow on purpose — one upstream host, only `api/v1/*`, traversal and absolute URLs refused rather than normalised, `x-api-key`/`Authorization`/`Cookie` stripped from what the caller sends so a browser cannot spend through it with a key of its own, and hop-by-hop response headers not replayed. Upstream 4xx/5xx bodies come back verbatim because the client already knows how to read them. Two things caught while migrating: six `pollForResult(requestId, key, …)` call sites still passed a local that no longer existed (a "key is not defined" toast in place of an image — now covered by a test that drives all three generating methods through a stubbed fetch), and an already-aborted poll used to resolve its route before giving up, which now touches the network zero times.
- **The picker says whether an account is connected, and repairs it there.** Reported: picking GPT Image 2 (OAuth) and pressing Draw answered *"OpenAI GPT Image (ChatGPT sign-in): Invalid refresh token."* — a sentence that is not an instruction, arrives after the press, and whose remedy was two pages away. Three things were wrong. **(1)** `oauth_provider_status("openai")` set `usable = connected` and `needs_reconnect = False` unconditionally, so a grant whose refresh token had been revoked reported as connected right up until generation; it now reads the same `usable` / `needsReconnect` / `error` fields the xAI branch already read, and treats an error beside a connected grant as one needing reconnection. **(2)** The failure carried no remedy: `ImageRouterError` now has `remedy` and `provider`, set when the message matches a re-auth marker (`refresh token`, `invalid_grant`, `expired`, `revoked`, …) AND the route's credential is actually a grant — a key provider never offers a reconnect it cannot perform — and `/api/media-studio/image` returns `{message, remedy, provider}` so a 400 can become a button. **(3)** Nothing showed state before the press: new `src/lib/providerReadiness.js` resolves every provider to `ready / connect / reconnect / key / browser-key / offline / unroutable` with the action that fixes it, and `ModelFitPicker` renders that on the row — "Sign-in expired · Invalid refresh token." with a **Reconnect** button beside it — and on the collapsed summary, so a hidden picker cannot conceal that the selected model will fail. Reconnect starts `/api/oauth/{provider}/start` and OPENS the authorize URL (a link someone has to copy is the same failure as an error they have to interpret). A status the studio could not read is treated as unknown-and-actionable, never as a pass. Generation failures go through the same path, so a grant that dies mid-session becomes a Reconnect toast rather than a provider sentence. The rule behind all of it is now written down as a project UX rule: never present a problem where the solution could be offered instead.
- **Every studio now generates through one dispatcher.** Following the mis-route below, the three studios that were left out — Image, Cinema and Video — were migrated too, so no studio calls `muapi.generate*`, `localAI.generate` or the Media Studio lane directly any more and the anti-regression test has no exemption list. It is a DISPATCHER, not an abstraction: the five transports genuinely do not take the same arguments (local carries outpaint and inpaint masks, a studio lane and a runtime mode; MUAPI carries a request-id callback and a per-model quality field; the lane carries reference rows, LoRAs and a job id), and flattening them would lose the half that does not fit — losing a mask silently is a worse bug than the one this replaced. So `runImage`/`runVideo` own exactly one decision, WHICH transport runs, and the caller's payload passes through untouched. `extra` is keyed by transport and doubles as the caller's declaration of what it built: a row that resolves to a transport the caller has no payload for is refused loudly rather than sent a shape meant for somewhere else. The row is the routing identity and now wins outright — `model`/`workflow_id` are applied AFTER the payload spread, so a stale model left in a payload can no longer redirect a run. Two bugs were caught during the migration itself: taking `setup.modelId` for the lane row would have run a different workflow than the composer configured (the lane id is `workflowIdFromHivemindModelId(...)` and reference mode overrides it again), and removing the `muapi` import from a studio that still used it for a poll and an upload left an undeclared identifier a bundler does not catch — now pinned by a test that fails if any studio uses a symbol it does not import.
- **One path for running a selected model, after a real mis-route.** Reported: picking GPT Image 2 under the *OAuth* provider and pressing "Draw the plate" opened the MUAPI API-key dialog. Root cause: the Story studio — and the Sprite studio it was copied from — dispatched on `model.source === 'local' ? localAI : muapi`, so all ten of the catalog's cloud providers were treated as MUAPI. Three of them offer a model literally called `gpt-image-2` (OpenAI API key, OpenAI OAuth, MUAPI) on three different accounts, so with a key present this would not have errored — it would have billed the wrong account for a different model of the same name, invisibly. Fixed in one place on each side: `src/lib/modelRunner.js` is now the ONLY image-generation entry point a studio may use, declaring per catalog provider whether it runs through the local bridge, the browser's MUAPI client, or the server; and `src/hivemind_content_studio/image_router.py` holds the server-side provider → generator table behind a new owner-only `POST /api/media-studio/image`, which requires a `provider` (a model id alone is ambiguous) and refuses an unrouted one rather than defaulting. Only the MUAPI key lives in this browser, so only a MUAPI pick asks for a key here; every other credential is checked where it actually is and its failure names the account to fix ("OpenAI GPT Image (ChatGPT sign-in): …") rather than returning a bare 401. Rows the studio cannot route are marked unavailable in the picker *with the reason*, instead of being offered with a Draw button that can only fail. Two mechanical guards: a Python test asserts every catalogued image provider has a route (a new provider is a loud KeyError, not a silent default), and a frontend test forbids any studio from calling `muapi.generateImage`/`localAI.generate` directly — with Image, Cinema and Video listed as exemptions **by reason** (they pick from `modelsData.js`, which is the MUAPI catalog, so "not local, therefore MUAPI" is true of every row they can offer), plus a second test that fails if one of them ever starts reading the server catalog and invalidates that reason. Those three were migrated in the follow-up above.
- Auto-fill, added on request: a **wand on every input** (62 of them) writes that one field, and a **"Fill N blanks" button in every stage header** writes the whole section. Both read the WHOLE production as context, not the stage in view — a caption's hook is written knowing the signature detail six stages upstream, and a location's weather knowing the emotional turn. The field being written is omitted from its own context, because a field offered as evidence for itself comes back unchanged and the button looks broken. A section fill asks only for what is BLANK and leaves written fields alone (the button is disabled and says "Nothing blank" when there is nothing to do); a single wand always rewrites, since pressing one field's own button is explicit intent. New `src/studios/story/fields.js` is the one registry of what each field IS — the studio renders its label and hint from it and the producer is told the same guidance, so the two cannot drift; it also owns `readPath`/`writePath` (list-aware, structurally sharing untouched nodes), `storyContext()`, and `acceptedValues()`, which refuses a value for a field nobody asked for and refuses prose in a field with a fixed option list. One new producer task, `fill`, serves both cases — a single field is a fill of one. It is instructed never to contradict a locked contract, an identity lock or a never-change list, and to leave a field out rather than invent a fact the rest of the production has to live with.
- Fixed after the first real run (2026-08-24, reported: the button went disabled with no spinner, and several minutes later "the producer did not answer with JSON"): **(1)** the busy state was one string — `setThinking(task)` for the spinner, then overwritten by `askProducer`'s own `onStatus`, so `loading={thinking === 'concepts…'}` went false on the first status update and the button dropped its spinner while staying disabled. `busy` (which ask is running) is now separate from `thinking` (what it is doing), every producer button shows its own spinner, the status text and a Cancel render beside the button that asked, and the concept empty state says what is happening instead of "No concepts yet". The image draws had the same shape of bug on a smaller scale: two character cards both tested `drawing === 'character sheet'`, so drawing the second one spun the first one's button — the busy key is now per card. **(1b)** and the fix for (1) shipped its own bug: `ProducerStatus` bailed with `if (busy !== task)`, but both sides are EMPTY at rest — nothing running is `busy: ''` and a concept card that is not the one asking passes `task: ''` — so `'' !== ''` was false and every idle card grew a permanent "Working…" with a Cancel beside its Lock button. The predicate is now `producerIsRunning(busy, task)` in `story/state.js`, which requires a task before it compares anything, and is unit-tested rather than left inline. **(2)** `story_producer` never overrode `local_llm.chat`'s shared `max_tokens=2048`, which eight concepts of six prose fields do not fit, let alone a sixteen-panel board — and a reasoning model's `<think>` block is charged to the same budget. Overrunning it does not error: llama-server returns truncated text with `finish_reason: "length"`, which arrived here as unparseable JSON after minutes of waiting. Budgets are now per task (6000 for concepts and boards, down to 3000 for a compression) with a 900s timeout, since 180s does not cover a 6000-token answer and a timeout reads exactly like a hang. **(3)** Truncation is now recoverable and legible: `extract_json` scans braces string-aware rather than taking the last `}` (a `{` inside prose used to make a complete answer look cut off), drops an UNCLOSED `<think>` opener — which `local_llm._strip_reasoning` leaves behind, since its regex needs a closing tag, so a model cut off mid-reasoning handed the brace scan a half-drafted JSON object to mine — distinguishes "ran out of room" from "wrote prose", **salvages the list elements that finished** (six concepts out of eight is real work, and discarding it after minutes of local inference was the worst available outcome), returns them with a note the studio toasts, and retries a cut-off answer by asking for something SHORTER rather than repeating the ask at the same size. Error messages now carry a snippet of what actually came back; "the producer did not answer with JSON" was true, unactionable and indistinguishable from four other causes.
- **"Connect HivemindOS" asks the app instead of reading its files.** Built on the design you sketched. The studio mints a one-time link and opens `hivemindos://models/link?nonce=…&callback=…`; the app foregrounds, shows *"Hivemind Content Studio wants to use your HivemindOS credits"* behind its own auth, and on Allow **its server** posts the key to the studio's loopback callback — so the key never passes through a webview and never leaves the machine. One finding worth stating: a custom scheme resolves on the computer the browser is on, so this is **same-machine by construction** and adds no reach over reading the vault. What it adds is consent, and a handover that keeps working if HivemindOS ever moves that vault somewhere this studio cannot read — an OS keychain, or another user's account. Both are good reasons to have it; neither is "cross-machine", and the studio refuses to offer the button at all when it is being viewed over the tailnet, where the link would ask the wrong computer. The callback is the one route added to the sign-in gate, because the poster is the app and cannot hold a studio session: it is guarded by loopback-only callers, a 32-byte single-use nonce minted here in the last five minutes, and verification of the key against HivemindOS before anything is stored — and unknown, expired and already-used nonces all answer identically, because telling an unknown local process which of those it hit is telling it how to try again. A gate-list test pins that this is the only addition. **The button is never dead**: a scheme nothing handles fails silently, so the studio treats silence as an answer after 45s and says *"HivemindOS did not answer. Open it and try again, or paste an account key below."* with the paste path still open underneath.
- **A HivemindOS install on this machine links itself.** Asked directly: can it not just detect the app? It can. The app keeps its account key in `~/.hivemindos/hivemindos-model-credit-vault.json`, AES-256-GCM under a SHA-256 of the sibling `…vault.key` file, both owner-only — the same directory this studio already reads its device token and shared env from. So the studio reads it and spends the same balance with nothing to paste. Read **live rather than copied**: a copy would go stale the moment the app rotates its key, and keeping two files in step is how one of them ends up wrong. This grants no capability the studio did not already have — with the app running it proxies through it and spends that balance anyway — what it adds is that the link **survives the app being closed**, and that installing HivemindOS later links on the next refresh with no further step. It is said out loud rather than adopted quietly: the picker reads *"Linked from the HivemindOS app on this machine — the same balance, even while it is closed"*, and a key the owner connects by hand still wins over it, because connecting one is a choice and this is a convenience.
- **One HivemindOS balance, with or without the desktop app.** The step before this got the models right and the money wrong: without the app the studio bought its OWN credit account, and the picker said so ("this studio's own balance … installing HivemindOS later keeps its balance separate") as though a second wallet were an acceptable outcome. It is not, and the gateway never required it. A HivemindOS balance is a **credit account**, and `X-HivemindOS-Credit-Token` accepts either that account's own key or a passkey-minted session key (`hmos_credit_…`) — `rawCreditAccountByTokenHash` resolves both to the same account. So the studio now holds the OWNER'S key and spends the credits they already have: **Connect account** on the HivemindOS tab takes the key, verifies it against the gateway's balance route before storing anything (a key saved and first tried later fails in the middle of a generation someone was waiting on), and keeps it encrypted on the machine with the Keychain-backed cipher — never back to the browser. Everything downstream follows the connected account: chat spends it, the balance line reads it, and a card top-up **presents it** so the gateway credits that account instead of minting another. For the one case this cannot prevent — credits bought here before connecting — `POST /api/hivemindos/models/merge-credits` folds the balances through the gateway's own `credits/consolidate`, because stranding money in an account nothing can see is the worse answer. With the app running none of this is needed: it already holds the key, so the studio proxies through it exactly as before.
- **The HivemindOS app is not required for the cloud producer.** The first cut of this routed everything through the app on `127.0.0.1:5020`, which meant a user who has not downloaded HivemindOS got nothing — the picker showed the source greyed out with "Open HivemindOS", which is not an answer for someone who does not have it. That was wrong about the architecture: **the app is a proxy in front of a public gateway** (`hivemindos-paid-agent-gateway.hivemindos.workers.dev`, the same default its own `paid-agent-cloud-client` uses), so the studio now takes whichever of the two is there. `app` when it is running and linked, because that is what adds the machine's ONE shared credit pool, its free-tier device identity and the house tiers whose GPU-first routing lives in it; `direct` otherwise — the catalog (396 models), the free tier and paid inference all work with nothing installed. Model ids are identical on both routes, so a model chosen today is the same model after installing the app tomorrow. Three details that matter: the free rail is **device-scoped**, so the studio mints one stable device id per install and keeps it (a fresh id per call would be asking a shared meter for a new allowance every time); a prepaid **credit token** bought on the direct route is stored on this machine encrypted with the Keychain-backed field cipher at `0600`, never in the browser, exactly like the MUAPI key; and prices are formatted with the app's own formatter so the same model cannot appear to cost two different things in two of HivemindOS's products. "Add credits" is now two acts behind one button — with the app running it says to add them there so the balance stays shared, and without it the studio opens the gateway's own checkout — and the picker states which, because *"this studio's own balance; installing HivemindOS later keeps its balance separate"* is the kind of thing that should never be a surprise. Found while wiring it: Cloudflare **blocks urllib's default user agent outright** ("The site owner has blocked access based on your browser's signature"), which reads like an outage rather than a missing header, so the client identifies the product honestly instead.
- **The producer can think on HivemindOS's models, not only this machine's.** Requested: non-local options, an intuitive way to choose between local, cloud and HivemindOS, HivemindOS as the default when there are no local models, and gpt-luna as its default — on the same system and the same credits as the HivemindOS app. It is that app's own surface, called rather than reimplemented: `hivemindos_models.py` talks to the local HivemindOS at `/api/hivemindos/models/models` (the catalog — its six house tiers, the Hive Compute row and 384 gateway models on this machine today), `/chat/completions` (OpenAI-shaped inference, sent with the same `x-hivemindos-wallet-agent-id` funding identity and device token the dashboard sends itself) and `/credits` (the balance, read BEFORE a press). So routing (GPU first, OpenRouter fallback), the one shared credit pool per install, and the free tier's daily allowance are all HivemindOS's answers — there is no second catalog, no second economy and no provider key in this app. **Which engine runs a model id is one table** (`text_models.py`: the `hivemindos/` prefix, and everything else is local), for the reason the image side already learned — dispatching on anything looser is how an OpenAI pick reached MUAPI. `GET /api/text-models` returns both sources in one answer, each with its state, and the id to start on: a model already in RAM wins (free, private, answers now), else HivemindOS's default, else a local model that would have to be loaded cold. **The picker is three tabs** — On this machine · HivemindOS · Cloud models — with the recommended model (GPT-5.6 Luna) pinned to the top of the HivemindOS tab with a Default badge even though the catalog files it under the gateway, a search box over the 384 cloud models, each row's real price beside it (`$0.52 in · $3.12 out /1M`), the credit balance on the tab, and **the privacy sentence following the chosen model** rather than one line that is true of half of them — "stays on this machine" was being shown under every model and would have been a false promise the moment a cloud one was picked. Every state a source can be broken in carries its repair on the tab: no local models → **Open Models**, HivemindOS not running or not linked → **Open HivemindOS**, no credits → **Add credits**, and a refusal mid-ask keeps HivemindOS's own sentence with `remedy` attached so it becomes a button instead of a dead-end toast. Two things found by running it against the live HivemindOS rather than a mock: the free Scout model **refuses** any call asking for more than 1024 output tokens, and the producer's tasks budget 3000–6000 — so every stage button failed on the free model until the cloud engine clamped that budget (a short answer is already survivable; the producer salvages what finished); and this install has **no prepaid HivemindOS Models credits**, so the paid default answers `Add HivemindOS Models credits with card or link a local funding wallet before chatting.` with the top-up action — which is the remedy path working, not a bug.
- **A section fill no longer asks one question it cannot finish.** Reported (2026-08-24): pressing "Fill 17 blanks" on the Motion stage ran for minutes and left every box empty. That stage asks for the force, seven depths, camera, audio, negatives and two fields per beat — seventeen answers, most of them a sentence or more, all inside ONE JSON object. A local model that runs out of room part-way through an object does not return a partly-right answer, it returns an unparseable one, so the whole wait was discarded. Two fixes, on both sides. The studio now splits a section fill into asks a model can finish (`fillChunks`, split by weight rather than count — a one-line "Palette" and a paragraph of "Background atmosphere" do not cost the same), runs them in order, and **writes each one as it lands**: fields appear while the rest are still being written, each chunk sees what the earlier ones wrote (so the second half of a section cannot contradict the first), and a chunk that fails stops the run with what already landed kept — pressing Fill again then asks only for what is still blank. The report is honest about which of the three things happened: "Filled 3 of 4, then the producer stopped: …", "Stopped — 3 of 7 written." on cancel, or the plain error when nothing landed at all. And on the server, a cut-off `fill` is now salvaged the way a cut-off list already was: `salvage_pairs()` recovers the field/value pairs that finished — only string-to-string pairs, and only while each is the next thing in the text, so the half-written value is where it stops — and the "answer again, shorter" retry became per task, because *"give at most half as many entries"* is right for eight concepts and is the failure itself for a fill, where every field was asked for on purpose.
- **The storyboard sheet stopped coming back stretched.** Reported with the image: a four-panel board whose figures were visibly squashed sideways. Not the model's fault — it was asked for something no drawing satisfies. The prompt said "4 numbered **9:16** panels in a 2x2 grid" while the request asked for a **16:9** canvas, and the 2x2 cells of a 16:9 canvas are 16:9, so obeying the grid meant flattening every vertical composition to fit one. The two ratios were being decided in different files by different rules (`boardPrompt` used the clip's aspect; `drawBoard` hardcoded `'16:9'`, or `'1:1'` for sixteen panels). New `src/studios/story/sheetLayout.js` makes it one decision: it picks the arrangement and the canvas that fit the panels' own ratio, and reports the panel ratio **that canvas actually yields** — which the prompt then states, instead of the one that was wished for. A 2x2 of 9:16 panels is a 9:16 sheet; a 4x4 of them still is; two precision frames of a 16:9 clip stack rather than sitting side by side, and where no supported canvas can give the clip's ratio the prompt says what to compose for rather than silently contradicting itself. The concept contact sheet was fixed the same way — four directions across a square canvas was four slivers — and it now sizes its canvas to its own grid.
- Verification of the link handshake: 6 backend tests (a distinct one-time nonce and the callback carried in the deep link, a completion that stores the key and flips the state to linked, replay refused, unknown/expired/used answering identically, the callback open to a loopback caller with no session but refused from anywhere else, and the whole flow refused when the studio is not on this machine) plus a gate-list pin and 1 frontend test on the silence budget and its fallback copy. The handshake was driven end to end over the real functions — studio mints, the app's exact POST answers, the key lands, the replay is refused. In the browser: pressing **Connect HivemindOS** shows "Waiting for HivemindOS…", and against a stand-in app that answers, the offer disappears and the tab reads "1,200 credits left"; against one that never answers, the toast is the sentence above and the paste form is still there. App side: the deep-link handler, the parked cold-start request, the approval modal and the server-side handover route are written in `hivemindos` and pass `cargo check` and the full `tsc --noEmit`. **They are not built, packaged or committed** — per your standing rule, that goes through CI, so nothing in the shipped app answers this link yet.
- Verification of the auto-link: 2 backend tests plus a cross-implementation check — a vault written by **Node using the same primitives the HivemindOS app uses** (`createCipheriv('aes-256-gcm')`, base64url fields, SHA-256 of the key file) decrypts here byte-identically, so this is not a self-consistent round trip. An absent, unparseable or foreign-keyed vault reads as "not linked" rather than raising. In the browser, with no app running and its vault on the machine, the tab reads "1,200 credits left" and the linked-from line, with no connect form to fill in. `test/studio` and the frontend suites re-run below. The developer's real vault is pinned out of the test suite (`HIVEMINDOS_HOME`), like the app and the gateway before it.
- Verification of the one-balance rule: 4 more backend tests (a key verified against the balance route before it is stored and a malformed one refused where it was typed without a round trip, a connected account TOPPED UP rather than a second one opened, two balances folded into one with the survivor's key kept, and the paid refusal naming `connect-account` rather than "buy credits here") and 2 more frontend (the copy promising the same balance and explicitly not the old "own balance … separate" wording, and the credit line distinguishing "account not connected" from "no credits added yet" — different problems, different buttons). Driven in the browser on a machine with no app and no local models: the HivemindOS tab shows *"Connect your HivemindOS account to spend the credits you already have"* with the key field and, underneath, "No HivemindOS account yet? Add credits here"; pasting a key connects, toasts **"Connected — 1,200 credits."**, and the line becomes *"Your HivemindOS credits — the same balance as the app and every other HivemindOS app."* `test/studio`: **994 passed, 0 failed**. Frontend: **979 passed, 0 failed**. **Known rough edge**: the key has to be pasted, because WebAuthn's RP for these passkeys is `hivemindos.app` and the ceremony cannot run on the studio's own origin (the gateway's origin allowlist is https-only, so localhost cannot be added). The clean fix is a hosted connect page — passkey sign-in, then hand the session key back to a local callback — which is a change in `hivemind-cloud-services`, not this repo. Also unverified: that any existing HivemindOS surface displays the key for copying; the desktop app does not.
- Verification of the no-app route: proved against the REAL public gateway from this machine with the local HivemindOS pointed at a dead port — 396 models listed, GPT-5.6 Luna defaulted with its real price line, and `story_producer.produce()` answered a real `fill` task on the free tier (`{'location.weather': 'salt spray and a steady drizzle'}`) with **no app, no account and no credits**. A paid ask with no token refuses before it sends anything, with the `top-up` action. In the browser, a fixture serving that same real no-app catalog with the local source emptied — the case the question was about — starts on **OpenAI: GPT-5.6 Luna · HivemindOS**, tabs read 0 · 2 · 395, and the HivemindOS tab says "No install needed" over the free model and the paid default. 7 more backend tests (the direct catalog including the free row and excluding the house tiers it cannot route, the free rail's stable device id and non-urllib user agent, the paid path's credit-token header and upstream id, the token encrypted at rest, top-up refused while the app is running, and the route resolver treating "installed but closed" and "linked but absent" both as direct) and 3 more frontend (the route and where credits live, the tab copy never telling someone to install an app, and the badge counting what the tab lists). `test/studio`: **991 passed, 0 failed**. Frontend: **979 passed, 0 failed**. NOT verified: a completed card checkout and a paid completion actually spending credits — that needs someone to enter a card, so the checkout call is wired but was never fired. The test suite is now blocked from reaching the gateway as well as the app (`conftest` pins both), after the first run of these tests went out to the live service.
- Verification of the producer's two engines: 14 backend tests in `test/studio/test_text_models.py` (the prefix table, row normalisation, the gpt-luna default and its fallback when a gateway does not carry it, the OpenAI-shaped body with the studio's funding identity, the free tier billed as free-tier and clamped to 1024, each refusal's remedy, an unreachable HivemindOS as a rendered state rather than an exception, the starting-model rule, a broken source still listed, a cloud refusal keeping its repair instead of becoming a producer error, and both routes including the owner gate) and 10 frontend in `tests/textModels.test.js` (tab membership, the recommended model pinned, search by name and by id, the remembered choice surviving a reload and a deleted model not stranding the picker, the privacy line differing by source, per-source cost lines, the credit line, every remedy having a button, and a source assertion that the studio passes `source` through so a cloud model never enters the local load path). Driven against the REAL HivemindOS on this machine: the catalog returned 391 models, and `story_producer.produce()` on the free Scout model came back with a parsed `fill` answer — a real cloud producer answer, no credits spent. The paid default refused with `Add HivemindOS Models credits…` and `remedy: top-up`, because this install has no prepaid credits. Driven in the browser against a fixture serving that same real catalog: three tabs (6 · 7 · 384), Luna pinned as Default, `claude` narrowing the cloud list, the header and privacy line changing with the pick, "Showing 40 of 384 matches", and the three broken states each rendering its button. With the local source emptied, the producer defaulted to GPT-5.6 Luna as asked. The fixture's request log shows a cloud ask going straight to `/api/story/producer` with **no** `/api/prompt-helper/load` — the local load-and-wait is skipped, not faked. `test/studio`: **956 passed, 0 failed**. Frontend: **976 passed, 0 failed** (the 5 `hivemindStudioReferences` failures that were the standing baseline are fixed by other uncommitted work in this tree, not by this change). `vite:build` OK. NOT verified: a paid HivemindOS completion actually billing credits — this install has none, so the call refuses before it spends; the transport is the same one the free model just proved end to end. The prompt helper's own dialog is still local-only; it can read the same catalog when that is wanted.
- Verification of the two fixes above: 8 new frontend tests (the grid/canvas identity for 2x2 and 4x4, the panel ratio always being the canvas divided by the grid, a prompt that states no cell ratio at all, the compose-for line when it cannot, the contact sheet not being slivers, and the chunker keeping every field in order without exceeding its weight) and 3 backend (a cut-off fill keeping the pairs that finished and NOT asking twice, the scan stopping at the pair being written, and the shorter-retry never telling a fill to drop fields). Driven live on the fixture (5573) with a producer that echoes what it is asked: pressing "Fill 17 blanks" on Motion sent **6 asks of 3 fields**, each carrying 3 more context facts than the last (so chunk 3 saw chunk 2's answers), and the stage went to "Nothing blank"; with the fixture failing the second chunk, the toast read *"Filled 3 of 4, then the producer stopped: …"* and the three that landed stayed written, with the header offering to fill only what was left; a single wand press was still a single ask. The board request now carries `aspect_ratio: 9:16` with a prompt saying "4 numbered panels in a 2x2 grid on a single 9:16 canvas" — and switching the studio to 16:9 moved both together. Frontend 966 tests, 961 pass, the same 5 pre-existing `hivemindStudioReferences` failures. `test/studio`: **943 passed, 0 failed**. `vite:build` OK. NOT verified: a real image model drawing the corrected board (the fixture captured the request; the fix is in what is asked for, not in what comes back).
- Verification: 51 new frontend tests in `tests/storyMethod.test.js`, all passing, over every rule above. Frontend suite: only `hivemindStudioReferences.test.js` fails (5), the documented pre-existing failures at HEAD. Backend: 21 new tests in `test/studio/test_story_producer.py` (task declarations, fence and preamble repair, the pointed retry, each refusal, and the route: parsed answer, unknown task refused before any model is asked, a producer failure as a 400, owner-only) and 5 added to `test/studio/test_capability_matrix.py`; `test_sprite_pipeline.py`'s matrix assertion was loosened from equality to subset, since the route now serves five features. `test/studio` 868 passed with 2 failures, both pre-existing: `test_providers.py::test_openai_and_xai_media_readiness_uses_the_correct_auth_surface` (confirmed failing with this change stashed) and the sprite matrix assertion (now fixed). `vite:build` OK. The follow-up fixes add 12 backend tests (per-task budgets and the timeout actually reaching the model, unclosed vs closed `<think>`, a brace inside a string, cut-off vs prose, salvage with and without a `list_key`, the shorter retry not replaying the broken answer, and the route handing a salvaged answer over with its note) and 8 frontend tests (the client's `{result, notes}` contract, a loaded model not being loaded again, the idle/running predicate, and three source assertions pinning the exact regressions — no spinner gated on the status text, no two character cards sharing a draw key, and no bare `busy !== task` bail), and were driven live: the button now spins with "Thinking…" and a Cancel beside it from the first tick, and a salvaged answer renders its two concepts with the note toasted. Auto-fill adds 12 frontend tests (path read/write through lists, per-row field expansion, blanks-only section fills, whole-production context, a field omitted from its own context, empty values dropped, the ask format, and the two `acceptedValues` refusals) and 5 backend, and was driven live against a fixture that echoes back what it was asked: a single wand sent 6 context facts and wrote only its own field; the Location section fill asked for exactly its 7 blanks, left the written `place` alone, and flipped the header to "Nothing blank"; four `<select>` shot fields took a valid option each; and typing kept focus on every keystroke (the wrapper component is declared at module scope, so React never remounts the input under it). Live E2E on the fixture dev server (5573) against a fixture producer: concepts drafted and rendered as cards, compared and shortlisted from the ranking, one locked → contract sentence and two characters with full identity locks; a sheet drawn through the real `draw` path (muapi stubbed at `fetch`) landed as a persistent reference URL with the audit checklist beside it; location directions offered, one chosen, the plate prompt generated with both "no characters" clauses; the board prompt quoting the never-change lists rather than redescribing anyone; the motion script assembled from the drafted beats with no warnings; the gate failing `identity` and showing the characters-stage repair; "Open in the Video studio" navigating and landing the script in the video composer's prompt; and the whole production encrypted to the vault and restored byte-for-byte across two reloads. NOT verified: a real local LLM answering the seven tasks (the fixture supplied the JSON) and a real paid image generation (the muapi transport was stubbed) — both share the code the fixture exercised. No stack restart needed for the frontend; the new `/api/story/producer` route DOES need one.
- Intended commit message: `feat(studio): a Story studio — concept, sheets, location, board, motion and the gate, one decision at a time`

## 2026-08-22 15:52:00 +0800 — A reload brings back every tab's generation, not just Tab 1's

- Status: Uncommitted
- User-facing result: Reloading the page during a video generation no longer presents an idle studio over a machine that is still rendering. The whole tab strip comes back, each tab reclaims the run **it** started with its live progress bar, its model name and an elapsed clock that counts from the original submit rather than restarting at the reload — and when the render lands it lands in that tab's canvas and history. Three tabs rendering means three tabs restored, each polling its own job. It also no longer matters which page you reload on: a studio with unfinished work is mounted at boot, so a run started in the Video studio keeps going even if you reload while looking at History. Cancel now genuinely stops a resumed run instead of only clearing the screen.
- Root cause: three separate holes, all downstream of the same assumption that there is one tab. (1) `StudioTabs` opened with `useState(newTabState)`, so a reload dropped every tab but the first; the jobs of the others stayed in the pending-job registry with no tab left to own them and were never polled again. (2) The resume block took `localPending[0]` — one job, whichever came first — and was gated on `isPrimaryTab`, itself derived from "this tab has no seed", which a reload makes true of *every* restored tab. (3) A studio is loaded on first VISIT, so a reload that landed anywhere other than the studio's own page left its jobs unattended until the user happened to click it. Also on the resume path: no `AbortController` was assigned, so Cancel reset the UI while the poll kept running underneath, and the restored progress card showed no model name because only aspect ratio and duration were carried.
- Fix: The strip is persisted to `sessionStorage` per studio (`saveTabState`/`loadTabState` in `src/lib/studioTabs.js`, validated hard — distinct positive ids, `nextId` past all of them so a later tab can never be handed a live tab's id, and capped at 24 since each entry mounts a studio). Every saved job now carries `tabId` (and `modelName`), and `pendingJobsForTab()` in `pendingJobs.js` decides ownership: a tab resumes its own jobs and only its own, while the primary tab additionally adopts the ORPHANS — jobs whose tab was closed mid-render and jobs saved before tab ids existed — and polls those quietly into History, since there is no canvas of theirs left to restore to. `StudioTabs` now tells each studio its `tabId`, whether it is `primary` (first position, not a seeded copy) and the `openTabIds` it can check an owner against, because the old "no seed = the original tab" test stopped distinguishing anything once a reload restored every tab with a null seed. The video resume was widened from local-only to whichever kind the tab owns, takes an `AbortController` so Cancel stops it, and restores the model name and true submit time. The image studio gets the same per-tab ownership (its jobs are cloud-only) plus the live canvas for its own run, with the timing signature nulled so a resumed job's wall time is not filed as a measurement of the configuration currently on the panels. `App.jsx` consults the pending-job registry at boot and mounts any studio with unfinished work, hidden, so it can resume wherever you landed. The app-instance half of `studioLaneId` moved into `sessionStorage` too: it used to be minted per mount, so a reload moved every restored tab onto a fresh scheduler lane and the next generation would no longer queue behind the run it had just resumed.
- Verification: Frontend node tests 615 ran, 609 pass; the 6 failures are the pre-existing ones at HEAD (5 `hivemindStudioReferences` env failures + `a rejected drop reports WHICH failure it was`), confirmed byte-identical against a stashed baseline. 15 new tests: 7 over strip persistence (round trip, per-studio isolation, junk and empty input, the `nextId` guarantee, duplicate ids, the 24 cap, lane stability) and 8 over resume ownership (a tab claims only its own job, the primary tab takes the orphans and a non-primary tab takes none, junk registries) plus source assertions on the wiring. `vite:build` OK. Live E2E on the fixture dev server (5573) against a fixture control API whose jobs never finish: three tabs generating → reload → all three restored, each with its own progress card, its own model name and elapsed times matching its own submit (0:38 / 0:33 / 0:27), and the fixture log showing three distinct job polls with no double-polling. Reloading onto `page=history` resumed all three anyway (polls in the log, and the card showed 1:24 continuous when Video was opened). Cancel on a resumed run stopped its poll dead in the log. A planted job with a closed tab's id plus a legacy job with none were adopted by Tab 1 as "Resuming 2 pending generations…" without displacing its own live card. With the fixture set to finish, both tabs' resumed runs landed their clip in their own canvas and history and the registry drained to empty. NOT verified: the same flow against a real rented machine (that needs the owner passphrase gate), and the muapi cloud resume path (needs an API key) — those share the code the fixture exercised but were not run end to end. No backend change, so no stack restart is required; the 8765 app serves the rebuilt dist on reload.
- Intended commit message: `fix(studio): a reload restores every tab and the generation it was running`

## 2026-08-22 16:22:00 +0800 — Every character the studio can insert now says what it sounds like

- Status: Uncommitted
- User-facing result: Two ways a character could reach a prompt with no voice attached are closed. (1) Picking one from the **Character** menu with no cast set used to write only the likeness — "<Subject 3> is Kratos from the video game God of War (2018)." — and "as played by X" in that line is about how a character LOOKS, so the moment you wrote them a line they spoke in H3's default generic adult male. It now names the voice beside the likeness, and the timbre where the catalog knows it. (2) **77 characters got a timbre description** (`voiceQuality`), taking coverage from 10 to 87: the games and anime from the original reddit list, which are exactly the entries whose voice text names no performer and therefore had nothing to retrieve. Naruto now carries "a brash, scratchy boyish shout, loud and energetic — never deep or adult-sounding"; Paimon, Pikachu, Tails, Atreus and Sailor Moon all carry an explicit "never … adult" clause, because the thing being ruled out is the default. 22 entries still have neither, each for a stated reason — they do not speak in words (Link, Chewbacca, Samus, Doomguy, the Xenomorph, Nezuko), the player picks the voice (Commander Shepard), or nobody was confident enough to describe them.
- Root cause: `applyCharacterToPrompt` inserted `characterPromptText` and nothing else — the likeness form. And `voiceQuality` existed on 10 entries out of 473 while being the documented fallback for exactly the case the catalog is now full of: a name the model cannot place returns a generic adult male (measured twice, 2026-08-13 — an unattributed exhale and a named SpongeBob both came back as an older man).
- Fix: `characterSubjectLines()` exported from `castPrompt.js` and used by BOTH the cast compiler's `subjectDefinition` and `applyCharacterToPrompt`, so one wording defines a known character wherever it is written. It takes `speaker` plus an `unbilled` flag, because an empty speaker id means two different things: a member the compiler was told to leave SILENT gets no voice line at all (unchanged), while a character just quick-added has no line YET and gets `<Subject N> speaks in <voice>.` — deliberately with NO speaker id, since H3 numbers speakers by who talks first and guessing one is how two characters' lines get swapped. 77 `voiceQuality` strings written in the existing house style (lowercase, no trailing period, a "never …" clause where the trap is the default voice). Also fixed while verifying: the quick-add toast counted definition LINES rather than subjects, so Kratos was announced as `<Subject 5>` when he was 3.
- Verification: Frontend node tests 596 ran, 590 pass; the 6 failures are the pre-existing ones at HEAD, unchanged. 4 new tests: the quick-add writes the voice line and assigns no speaker id; every catalog entry either names a performer or describes a timbre, with the 22 exemptions listed by reason in the test itself so a future addition with neither fails loudly; and the childlike voices rule out the adult default. The compiler's silent-member behaviour was checked directly — a member left out of `speakingOrder` still gets no voice line. `vite:build` OK. UI on the fixture dev server (5773): the fight starter loaded with no cast, Character → Kratos wrote three lines (likeness, voice, "a deep, gravelly growl, quiet and controlled rather than shouted"), Character → Paimon wrote "a tiny, extremely high-pitched squeaky child voice — never deep or adult-sounding", and the toast named the right subject after the fix.
- Intended commit message: `fix(characters): a character inserted without a cast names its voice, and 77 entries gained a timbre description`

## 2026-08-22 15:34:00 +0800 — The character catalog grows from 189 to 473 off a measured usability index

- Status: Uncommitted
- User-facing result: The Character and Cast menus now offer 473 characters instead of 189. The 284 added are the ones malcolmrey's H3 usability index actually filmed and filed as usable — Tommy Shelby, Jack Bauer, Rick Grimes, Gandalf, Jules Winnfield, Don Draper, the whole Walking Dead / Supernatural / Brooklyn Nine-Nine / NCIS / West Wing benches, Arcane's Jinx and Vi, and five separate Batmen (Adam West, Clooney, Bale, Affleck, Pattinson) each carrying the actor and year that pick that one likeness. 33 of them come from the index's `onthefence` folder and say so in the row ("Mixed results in testing"); its 835 `bad` characters were not imported. Four entries that had no performer now name one: Darth Vader (James Earl Jones), Rick Sanchez and Morty Smith (Justin Roiland) and Jerry Seinfeld — which matters because a character whose voice the model cannot place comes back as a generic adult male.
- Root cause: not a bug — the catalog was built from a reddit thread that named characters without testing them, and the index is a measured replacement: one row per character, each backed by a test clip, 1,293 rows across good / onthefence / bad. No existing actor name disagreed with it; every one of the 48 overlapping entries that named a performer named the same performer.
- Fix: `src/lib/h3Characters.js`. The index's "Real Actor / Actress" is ONE column covering both kinds of performer and this catalog splits them, so it lands in `actor` for a live-action likeness and `voiceActor` for a drawn one (Arcane, Rick and Morty, South Park) — the split exists because naming the wrong performer is worse than naming none. `medium` and `year` are not in the index and were supplied per franchise (215 franchises). Deduplicated three ways: against what we already carry under a different name (Captain Jack Sparrow→Jack Sparrow, Tony Stark→Iron Man, Steve Rogers→Captain America, Bruce Banner→Hulk, Natasha Romanoff→Black Widow, Jean-Luc Picard→Captain Picard, Albus Dumbledore→Dumbledore, the T-800), against the index's own repeats (Django/Django Freeman, Aldo Raine/Lt. Aldo Raine, both Top Gun Mavericks, both Bond rows), and against the fictional-characters-only rule, which excludes its 78 real-celebrity rows plus the talk-show and sports hosts filed under a programme (Joe Rogan, Conan O'Brien, Gordon Ramsay, Kobe Bryant, Jon Stewart, Tucker Carlson, Letterman, John Oliver, Larry David, Dave Chappelle, Neil deGrasse Tyson, Robert Lewandowski, Trump, and the Fresh Prince "Will Smith"). Ron Weasley comes OFF the deliberately-absent list — the thread said he fails, the index filmed the Rupert Grint likeness and filed it under `good`. Where a name has several tested depictions (five Batmen, two Hannibal Lecters, two Sherlock Holmeses, two Lex Luthors, two Max Rockatanskys) each is its own entry under its own series, which is how the menu and the source clause disambiguate them. The eight new `series: 'Marvel'` entries were given an `origin` clause in the group's existing style, because "the film series Marvel (2012)" names nothing the model can look up.
- Verification: Frontend node tests 593 ran, 587 pass; the 6 failures are the pre-existing ones at HEAD, unchanged. 3 new tests pin the merge's rules: a drawn character never carries `actor` (asserted over the whole catalog) while Darth Vader legitimately carries `voiceActor` with a live-action medium; no real person leaked in and Jerry Seinfeld's scripted-self entry survives; the five Batmen are five entries under five series and The Dark Knight's renders as "Bruce Wayne / Batman as played by Christian Bale from the film The Dark Knight (2008)". The catalog-size floor in the existing well-formedness test went 120→470, and that test already asserts every entry can state a source with a year — which is what proves all 284 got a usable `medium`+`year`. `vite:build` OK. UI on the fixture dev server (5773): the Character menu renders the new rows with actor and year, the `onthefence` hint shows on the row, searching "Batman" returns the five era-distinct Batmen plus the Riddler/Catwoman/Gordon/Luthor rows, and the Cast panel's own picker finds new entries (Dr. Walter Bishop, Fringe) — menu open stayed at 1ms for the click handler with 473 entries loaded.
- Intended commit message: `feat(characters): merge malcolmrey's H3 usability index — 284 tested characters, and the voices four entries were missing`

## 2026-08-22 14:56:37 +0800 — Recasting a prompt rewrites the WHOLE prompt, not only the two sections the cast owns

- Status: Uncommitted
- User-facing result: Applying a cast to a prompt written for a different cast no longer leaves half of the old one behind. Loading "Fight a cartoon character" and casting yourself + Naruto used to rewrite the subject definitions to Naruto while the spoken line still asked for `<d>[English in SpongeBob SquarePants' voice from SpongeBob SquarePants as voiced by Tom Kenny] Ouch!</d>` — the definitions asked for one character and the line asked for another. The dialogue voice tag, the `(Sx)` speaker ids and any bare character name in the prose now all follow the cast that is attached. Two more doors into the same half-rewritten state are closed with it: a prompt LOADED from the library while a cast is set is recast on the way in (toast: "Prompt recast for your cast"), and the Character quick-add joins the cast instead of appending its source form past the end of the last section, where it landed inside `non_diegetic_music` and was rendered as a note about the music. Anything the new cast cannot fill — `<Subject 4>` with three members, `<Picture 3>` with no pictures attached — is now listed in the Cast panel instead of silently conditioning on nothing. The words inside `<d>…</d>` are deliberately still yours: a line that says "Take that, SpongeBob!" is reported, never rewritten, because a `<Subject 2>` substituted there would be read out loud.
- Root cause: `applyCastToPrompt` (`src/lib/castPrompt.js`) rewrote `subject_definitions` and `retention_analysis` from the cast and carried summary / detailed_description / overall_soundscape / non_diegetic_music verbatim, on the documented reasoning that the creative half is what the person wrote. It is not, quite: three things in there name a specific MEMBER rather than a subject position — the dialogue language tag (which is exactly where a known character's voice is named, by `dialogueTag()`), the `(Sx)` pairing (which moves whenever the speaking order does), and a bare name left by whoever wrote the template. All three survived every recast pointing at the outgoing cast. Two secondary gaps in the same control: `CastMenu.toCastMember` never passed `voiceQuality`, so the Cast button produced a WEAKER prompt than the starter it is supposed to reproduce (the catalog's "high-pitched, nasal, squeaky … never deep, gravelly or adult-sounding" fallback, which exists because naming a voice the model cannot place returns a generic adult male), and the cast lived in `useState` inside the menu, so nothing outside it could recast anything.
- Fix: A recast pass over the carried creative half. `readCastFromDefinitions()` reads the OUTGOING cast out of the prompt's own `subject_definitions` — a prompt carries its own cast, which is what makes this exact without remembering anything between two runs — and `recastCreative()` splits each section on `<d>…</d>`, rewrites every language tag through `dialogueTag()` for the subject that owns the line (the last `<Subject N>` before it, which is how both this module and H3's guide write an attributed line), re-points every `<Subject N> (Sx)` at the current speaker, and replaces the outgoing cast's names in the PROSE with the subject position they held. New warnings for a label the cast cannot fill (`<Subject N>` / `<Picture N>` / `<Video N>` / `<Audio N>` beyond the allocation) and for a name left in spoken words. `formatSixSections()` + `isSixSectionPrompt()` added as the inverse of `parseSixSections`. `applyCharacterToPrompt` (`h3Characters.js`) gains a six-section case: a character defines a subject of its own, numbered after the ones already there, instead of being appended past the last section. The cast itself moved out of `CastMenu` into the studio engine (`s.cast` / `s.castWarnings`, `members`/`onMembersChange`/`warnings` props, `castApplication()` + `characterCastMember()` exported), so `loadPromptText()` can recast a library prompt on the way in and `addH3Character()` can add to the cast. Both implicit paths change the PROMPT only and never touch the reference rows — loading a prompt or picking a cartoon is not a request to reshuffle what is attached.
- Verification: Frontend node tests 590 ran, 584 pass; the 6 failures are the pre-existing ones at HEAD (5 `hivemindStudioReferences` env failures + `a rejected drop reports WHICH failure it was`), byte-identical to the baseline taken before the change. 8 new tests: 5 in `castPrompt.test.js` over the recast (voice tag renamed in every section, a persona inheriting a spoken part losing the outgoing voice, a name in spoken words reported and not rewritten, speaker ids following a changed speaking order, dangling labels) plus 2 wiring assertions (voiceQuality reaches the compiler; the cast lives in the studio and the loader touches no reference field), and 1 in `h3Characters.test.js` over the six-section insert. `vite:build` OK. Headless repro of the reported prompt (the real starter + a 7-picture persona + Naruto through the real `characterPromptText`/`characterVoiceText`) comes back with `[English in Naruto Uzumaki's voice from Naruto]` and no SpongeBob anywhere. UI E2E on the fixture dev server (5773 + a catalog fixture on 9931 built from the live `media_catalog()`): MiniMax H3 selected, "Fight a cartoon character" loaded (6 SpongeBob mentions), cast Goku + Naruto added through the real menu and applied → 0 SpongeBob, all three tags recast; re-loading the same starter with the cast set arrived already recast with the toast; the Character quick-add put Shrek in as `<Subject 3>` with `non_diegetic_music` still ending cleanly at "none"; loading "Arcade versus match" onto the 3-member cast surfaced both dangling-label warnings in the panel. NOT verified live: the persona half of a cast (the saved-persona library needs the unlocked owner vault, which this session does not enter) — covered by the node tests and the headless repro instead. No backend change, so no stack restart is required; the 8765 app serves the rebuilt dist on reload.
- Intended commit message: `fix(studio): a recast rewrites the whole prompt — the voice in a dialogue tag, the (Sx) ids and the names in the prose follow the cast`

## 2026-08-22 11:30:00 +0800 — "Run on" is locked per studio tab, and two tabs can drive two rented machines at once

- Status: Uncommitted (stack restart still pending — see Verification)
- User-facing result: The "Run on" picker under the Rented source now pins THIS TAB to a machine. Picking the RTX 5090 in tab 1 and the RTX PRO 6000 WS in tab 2 leaves tab 1 on the 5090 when you switch back (it used to flip to whatever was picked last, in any tab), and a generation from each tab lands on its own box — both boxes work at the same time. A fresh tab follows the Machines view's default until you click a machine ("in use" → click → "locked"); an idle or broken box is attached on click, then locked; a lock whose machine is detached or destroyed is dropped on the next refresh instead of being sent. The Machines view's "Use" still sets the global default that un-pinned tabs and agents follow.
- Root cause: The picker was not per-tab state at all. A click POSTed `/api/gpu-rentals/{id}/select`, which rewrote one integer — that machine's `priority` — in the single attachment registry (`rental-lanes.json`), and the media gateway routed EVERY `/prompt` by graph content against first-match lane rules sorted by that priority. Nothing in an image `/api/generate` or a hivemind video request named a machine, so there was exactly one "selected" machine for the whole studio; each tab's picker honestly re-synced to it (the change event fans out to every mounted tab), which is what read as "not staying locked on its tab".
- Fix: A per-request pin, `run_on` (the rental id the studio shows, e.g. `vast:48352597`), carried on every generate path and honoured by the gateway. Gateway (`packages/media-gateway/app.py`): the registry reader keeps each lane's `rental_id`; `comfy_lane_for_pin` resolves a pin to an attached rental lane; `comfy_lane_for_prompt_body(body, run_on=…)` tries the pinned lane's rule FIRST, ahead of the priority order — the same thing `/select` did, scoped to one request — falling through to the normal order when the pinned box does not serve the graph (a pin settles which of several capable boxes runs a job, it never sends a model to a box that lacks it), and raising `ComfyLanePinError` for a pin naming a detached machine, which every entry point turns into `409 {error, operational: true}` so it survives machine-private redaction. Honoured by the `/comfy/api/prompt` proxy (top-level `run_on` or `extra_pnginfo.runOn`), `/api/lanes/resolve` (so the H3 motion-reference budget is priced against the pinned lane), `/api/generate` (refused up front on a stale pin, else rides in `options` to `run_comfy_api_image`) and `/api/upscale`. MCP (`media-studio-mcp.mjs`): `run_on` on `media_generate_video`, passed to the resolve guard and set top-level on the submit body (ComfyUI ignores the key); a 409 from the guard is rethrown, not swallowed. Control API / `media_studio.py` / `provider_execution.py`: `run_on` on `MediaStudioVideoBody`, `start_video`, `generate_video` and the agent pipeline options. `hosted-server.js` forwards it on `/local-ai/generate` and `/local-ai/upscale`. Frontend: `rentedMachineId` lives with the tab's settings (image engine field + `IMAGE_TAB_FIELDS`; video `setup`), persists with the other preferences (`imagePrefs.js`, `videoPreferences.js`, `applyRestoredPreferences`), is sent as `run_on` only while the source is Rented (all five `localAI.generate` calls, upscale, the hivemind video request), and `RentedSourceStatus` is now a per-tab picker (`pinned`/`onPin` props, `withPin()` in `rentedMachines.js` for the highlight) that no longer POSTs `/select` — an idle/broken row is plain-`/attach`ed then pinned, and a stale pin is dropped only against a non-empty machine list (an unreachable API is not "gone").
- Verification: Gateway unittest: 7 new tests (pin beats priority; pin that does not serve the graph falls through; stale pin raises; body parsing; over HTTP: `/api/lanes/resolve` + proxy submit route by the pin and refuse a stale one 409/operational; `/api/generate` refuses a stale pin before queueing and carries a live one into the runner) — 199 ran, the only 4 errors are the pre-existing `cryptography`-missing env gap on brew Python 3.14 (dual-seal/harvest tests). Studio Python: 365 passed (`test_media_studio`, `test_media_studio_mcp_contract`, `test_control_api`, `test_gpu_rentals_api`, `test_comfy_lanes`, `test_private_access`) incl. new run_on assertions at each layer. Frontend node tests: 582 ran, 576 pass; the 6 failures are pre-existing at HEAD (5 `hivemindStudioReferences` env failures + `referenceLabels` #21 whose regex target is absent in HEAD's `ReferencesMenu.jsx`); new `rentedMachines.test.js` cases cover `withPin`, the studio wiring and the no-`/select` contract. `vite:build` OK (8765 serves the new dist on reload). UI E2E on the fixture dev server (5573, two fixture rentals mirroring the live ones): tab 1 → lock 5090, tab 2 → lock PRO 6000, switch back → tab 1 still 5090, tab 2 still PRO 6000; Generate from each tab captured `run_on: vast:48352597` (tab 2) and `run_on: vast:48348132` (tab 1) on `/api/media-studio/video/start`, zero `/select`/`/attach` calls. REAL E2E on the two live rentals through an isolated gateway + MCP on this tree (ports 8917/8918, live `rental-lanes.json` + live SSH tunnels, the live stack untouched): `media_studio.start_video(run_on="vast:48352597")` → route record lane `rental48352597` and the prompt queued on the PRO 6000's ComfyUI while the 5090 (global priority 7, the leader) got nothing; a second job `run_on="vast:48348132"` → `rental48348132`; both completed `status success` on their pinned boxes (ComfyUI `/history`). Their harvest failed only in that harness — the throwaway requester key was P-256 and `media_seal.py` wraps with RSA-OAEP — so the clips were left on the boxes for teardown. NOT done: `zimage-stack restart` to load the new gateway/MCP/control-API/hosted-server — held because another session was actively editing `gpu_rentals.py`/`runpod.py` in this tree (mtime minutes old) and a restart would load that unreviewed code and interrupt any provisioning it is driving. Until the restart, the live 8765 app already serves the new picker but the OLD backend ignores `run_on` (pydantic drops the unknown field) and the picker no longer changes the global priority — route with the Machines view's "Use" in the meantime, then `zimage-stack restart` once the tree is quiet (idle queues confirmed at 11:19; both rental tunnels are detached sessions and survive it).
- Intended commit message: `feat(studio): "Run on" is per tab — generations carry a run_on pin the gateway tries ahead of the priority order`

## 2026-08-22 09:57:20 +0800 — A second workspace's History lists its own studio clips instead of nothing

- Status: Uncommitted
- User-facing result: A non-owner workspace that generates a video in the studio now finds it in History (the clip used to appear only in the studio's in-session strip and History stayed empty). In the other direction the owner's History no longer enumerates a sibling workspace's clips — each workspace sees its own gateway outputs, the owner additionally everything unclaimed (pre-accounts outputs, agent/machine-token jobs, clips of a since-deleted workspace). A clip generated BEFORE this change carries no claim and therefore still files under the owner; claim it for a workspace with `GatewayOutputClaims(data_dir / "gateway-output-claims.sqlite3").claim_output(<output name>, <account id>)` (first claim wins).
- Root cause: History's Canvas section is fed by `/api/canvas/history`, which indexes the media gateway's MACHINE-wide job log and output roots. With accounts (2026-08-21) that whole surface was declared owner-only on the assumption that a workspace's own studio generations land in its per-account `generated/media-studio/` store — but an E2E-sealed gateway output never does: `finish_video` cannot decrypt it, returns `gateway_output`, and the clip stays at the gateway. So a sibling's every video lived on the one surface its History was forbidden to list; `data/accounts/2/generated/media-studio/` was empty while the gateway held `cmf-8f830487-…mp4` sealed to that browser's key. The gateway itself cannot scope the listing: the requester key it records names a DEVICE (one browser's `X-E2E-Requester-Pub`), which two workspaces signed in from one browser share.
- Fix: `GatewayOutputClaims` (account_scope.py, beside `RunClaims`, same first-claim-wins ledger) records which workspace each gateway job (`/api/media-studio/video/start`, by job id) and each finished output (`_finalize_media_studio_video`, by logical filename) belongs to. `/api/canvas/history` now syncs only the gateway records the signed-in scope may see — `claim_visible()`, the rule runs already use — and purges from the workspace's store any record it may not (`CanvasHistoryStore.forget`), so a row the owner adopted in the seconds before the claim was written does not linger. The owner-only short-circuit and its `clear()` are gone; the detail routes (`/media`, `/workflow`, `/provenance`, DELETE) resolve through the per-workspace store, so a sibling's ids stay 404 for everyone else. The filename is the key that every gateway listing agrees on: a rental-lane harvest is listed under `file-<sha>` (the workflow index never knows it), so a job the studio never got to finish (restart mid-run) is found by id only on a local lane and otherwise stays with the owner as unclaimed.
- Verification: `test_account_api.py::test_canvas_history_shows_each_workspace_only_its_own_gateway_outputs` drives the real start/poll path as the sibling and asserts both directions plus the forget race; ledger + `forget()` unit tests in `test_account_scope.py` / `test_private_access.py`; `test_account_api.py`, `test_account_scope.py`, `test_private_access.py`, `test_control_api.py` pass 134/134. Live: an isolated throwaway studio (`python -m hivemind_content_studio.control_api` on 9765, scratch state) reproduced the rule over HTTP — owner saw both files while unclaimed, after the claim the owner listed only its own and workspace 2 only its clip, cross-workspace `/media` 404. Rolled out with `zimage-stack restart` (gateway idle, attached rental tunnel pid 78811 survived); studio healthy at 09:56:16 with `data/gateway-output-claims.sqlite3` created on boot.
- Intended commit message: `fix(accounts): History lists each workspace's own gateway clips — claim gateway jobs/outputs per workspace`

## 2026-08-20 15:10:00 +0800 — A rental that stops answering is reaped instead of billed behind "Booting host"

- Status: Uncommitted
- User-facing result: The Machines view no longer rewinds to "Booting host" when a provisioning poll times out; it shows the box's last known step and, past 45s, says how long contact has been lost. A managed box that Vast reports running but that has answered nothing for 15 minutes is now called an error and reaped, the way a container that never boots already was.
- Root cause: `_fetch_beacon` gave the on-box beacon 1.5s, and `_instance_dto` answered every miss with `step: "booting"`. The beacon is a single-threaded `python3 -m http.server` on a box saturating its own uplink over 8 ranged connections per file, so misses were the norm, not the exception. Measured on rental vast:48183103 mid-download: it answered in 0.54s when it answered, but 21 of 24 polls got nothing within 8s. The cosmetic half was the rewinding ladder. The expensive half was that `reap_failed_rentals` reads through the same timeout, so a host whose network dies — killing the downloads and the beacon together — reported `provisioning`, not `error`, and kept billing. vast:48183103 died at 3/5 on the 21GB transformer and was only destroyed because a poll happened to land in one of its brief responsive windows.
- Guards: A `ready` box is never reaped over a quiet status file — it answers through its tunnel and its ComfyUI. Silence is measured from process start when the in-memory cache has nothing, so restarting the stack beside a slow download cannot destroy it. A box with no published beacon port is not judged on silence at all.
- Verification: 6 new tests in `test/studio/test_gpu_rentals_api.py` cover the rewind, the surviving error reading, the silence escalation and its three guards; that file passes 111/111 and the complete studio suite passes 639/639. Live: a replacement 5090 (vast:48186258) provisioned through the patched path 0/7 -> ready in 3m05s with zero rewinds. The staleness path itself was exercised by tests, not by that box — its beacon answered every poll.
- Intended commit message: `fix: reap a rental whose beacon dies instead of billing it`

## 2026-08-20 14:16:49 +0800 — The studio names the broken funnel stage instead of leaving operators to read ratios

- Status: Uncommitted
- User-facing result: `hivemind-content-studio metrics diagnose <manifest...>` and the new `diagnose_content_bottleneck` MCP tool walk reach -> retention -> intent -> conversion -> economics over metrics the studio already records and name the first stage that underperforms, with the evidence and the next controlled test. A portfolio call also ranks vanity leaks: runs taking a large share of the portfolio's views while returning a much smaller share of its conversions.
- Calibration: Every band comes from the operator's own measured runs (portfolio medians, tunable `--underperformance`), plus an optional explicit `--roas-target`. The creator-network view bands that circulate as growth advice are vendor claims and are deliberately not encoded; a run without comparable history returns `unknown` rather than a verdict. Stage ids match the shared creator-rewards diagnostic ladder (CRD01-CRD05) so a studio diagnosis and a campaign brief use one vocabulary.
- Privacy boundary: The MCP tool returns stage verdicts, ladder ids, suspects, next tests, and run ids only. Observed views, revenue, and manifest paths stay local to the CLI, asserted by a test that serializes the tool's real response and fails on either.
- Verification: New `test/studio/test_diagnostics.py` passes 7/7, including the MCP redaction boundary exercised through `call_tool`; the complete studio suite passes 633/633; a scratch three-run portfolio through the real CLI flagged the 180k-view/3-conversion run as `intent` (CRD03) and as the only vanity leak.
- Intended commit message: `feat: diagnose the first underperforming funnel stage`

## 2026-07-16 16:55:34 -0400 — Video progress clears the composer and completion ping unlocks reliably

- Status: Uncommitted
- User-facing result: Starting a video generation now removes the Studio hero and composer from the active layout, places the animated progress card near the top of the viewport above setup controls, and restores setup only on Back or an error. Enabling Ping when complete immediately plays a preview tone; successful generations play the same louder two-note tone when the result becomes viewable.
- Root cause and audio handling: The composer retained a fill-mode entrance animation at `z-40`, which could override an opacity utility while the progress layer sat at `z-30`. The progress layer now uses structural hiding and `z-50`. The embedded Explore frame explicitly allows autoplay, and Generate primes Web Audio with a silent source during its user gesture so delayed completion playback remains authorized.
- Verification: OpenGen regressions pass 27/27; Studio UI contract passes 21/21; complete Studio suite passes 185/185; JavaScript syntax, production build, live served-bundle/CSS inspection, and `git diff --check` pass. Real speaker playback and screenshot QA were unavailable in this runtime.
- Intended commit message: `fix: keep video progress above composer and unlock ping`

## 2026-07-16 16:51:04 -0400 — Video model and duration survive reloads

- Status: Uncommitted
- User-facing result: Video Studio now restores the last selected video model and duration after a reload, including local Hivemind workflows such as LTX 2.3 Eros Fast once asynchronous workflow discovery completes.
- Privacy boundary: The persistent browser preference contains only the model ID and numeric duration. Prompts, reference images/videos, filenames, generation context, and outputs remain excluded.
- Verification: OpenGen regressions pass 27/27 (baseline 26/26); Studio UI contract passes 21/21; complete Studio suite passes 185/185; JavaScript syntax, production build, live served-bundle inspection, catalog ID-collision audit, and `git diff --check` pass. Browser interaction QA was unavailable in this runtime.
- Intended commit message: `fix: persist video model and duration`

## 2026-07-16 16:40:05 -0400 — Regenerate skips the hidden editor

- Status: Uncommitted
- User-facing result: Regenerate now restores the prior prompt, reference media, model, and parameters off-screen, pauses the previous result, and transitions directly into the generation progress card. The chat input appears only when Back to setup is chosen or regeneration fails and needs editing.
- Verification: OpenGen regressions pass 26/26; Studio UI contract passes 21/21; JavaScript syntax, production build, live served-bundle check, and `git diff --check` pass.
- Intended commit message: `fix: keep editor hidden during video regeneration`

## 2026-07-16 16:38:26 -0400 — Raised video progress card and completion ping

- Status: Uncommitted
- User-facing result: The animated video generation card now sits higher in the Studio viewport instead of rendering below the composer. A new Ping when complete toggle beside Generate plays a short two-note chime when a newly generated video becomes ready.
- Behavior and privacy: The toggle defaults off and is remembered only in tab-scoped `sessionStorage`. Audio is primed from the user’s toggle or generation click so asynchronous completion can play reliably. The completion guard fires once per generation and does not ping when opening an existing history video.
- Verification: OpenGen regressions pass 26/26; Studio UI contract passes 21/21; complete Studio suite passes 185/185; JavaScript syntax, production build, served-bundle checks, and `git diff --check` pass. The live OpenGen service serves the raised-card CSS and completion-audio code with all managed listeners online. A real render/audio playback and screenshot QA were not run because browser execution was unavailable in this runtime.
- Intended commit message: `fix: raise video progress and add completion ping`

## 2026-07-16 16:32:08 -0400 — Video generation progress and reversible result editing

- Status: Uncommitted
- User-facing result: Video generation now replaces the faded composer with an animated generation card showing the selected model, start-frame preview, requested format, elapsed time, a moving progress bar, and safe generation stages. Wan2GP telemetry drives a real percentage; Hivemind and remote providers remain honestly indeterminate when their API exposes no percentage.
- Result workflow: The completed-video view now has a Back to setup action that restores the same prompt, model, start/end frames, uploaded video, and generation options. Regenerate restores that in-memory submission before resubmitting, so it no longer fails with a hidden “prompt required” validation error. New still clears the setup intentionally.
- Privacy boundary: The submission snapshot and start-frame object URL remain memory-only and are never added to persistent Hivemind browser history. Raw provider progress messages are not rendered; the card maps telemetry to bounded status labels so local paths or operational output cannot leak into the UI.
- Verification: OpenGen JavaScript syntax and all 26 OpenGen regressions pass (baseline 23/23); the Studio UI contract passes 21/21 (baseline 20/20); the complete Studio suite passes 185/185; the production OpenGen bundle builds; `git diff --check` passes. The managed stack was restarted and all required listeners are online; the live OpenGen server serves the new Video Studio chunk and progress animation CSS. Screenshot QA was skipped because the in-app browser execution tool was unavailable in this runtime.
- Intended commit message: `fix: add video progress and preserve generation setup`

## 2026-07-16 16:00:20 -0400 — Creative reads require the unlocked owner session

- Status: Uncommitted
- User-facing result: Prompt history, Canvas workflows/media, generated Media Studio videos, OpenGen outputs, simple-plan results, and run artifacts are now explicitly owner-session-only. A control-token caller may still start or manage an allowed run, but receives a machine-redacted receipt with no prompt, QA payload, output filename, media URL, or private failure detail.
- MCP boundary: Machine-private Media Studio MCP calls now ignore `include_urls=true`, remove every media/output URL field, and return generic errors. The trusted Content Studio server resolves completed native or Comfy outputs through its private gateway credential, downloads them internally, and then uses the existing encrypted-at-rest owner playback flow.
- Verification: complete Studio suite passes 183/183; Media Gateway suite passes 41/41; JavaScript syntax, Python compileall, and `git diff --check` pass. The settled managed-stack restart has all required listeners online. Live probes confirm control-token media reads return `401`, an owner session decrypts the same temporary encrypted probe with `Cache-Control: private, no-store`, the probe is removed, live MCP history/job calls return no creative or credential fields even with `include_urls=true`, and all seven local video workflows remain discoverable.
- Intended commit message: `fix: make creative content owner-session-only`

## 2026-07-16 15:02:00 -0400 — Creative inputs and outputs stay behind the owner unlock

- Status: Uncommitted
- User-facing result: In the embedded Studio, image/video prompts and generated media history are now memory-only; pending remote jobs are tab-session-only; locking the Studio clears current and legacy creative browser state. Canvas workflow keys are stored in `sessionStorage` instead of persistent `localStorage`, while prompt-helper reference images remain AES-GCM ciphertext in IndexedDB.
- Media handling: Private image/video playback now encrypts a completed plaintext output before serving it, uses `Cache-Control: private, no-store, max-age=0` through both gateway layers, fails closed and removes the plaintext result when output encryption fails, and no longer prints bearer tokens or token-bearing request URLs in logs. Temporary Media Studio start frames and QA frame extracts are deleted after terminal generation, with a bounded stale-input sweeper as recovery.
- Security boundary: Owner cookies authorize creative reads; control tokens authorize only bounded machine operations with redacted receipts. Local model/runtime processes necessarily receive prompt and media plaintext transiently while generating. Output and workflow encryption protects durable state; this is not zero-knowledge execution against the local runtime itself. An explicit `ZIMG_OUTPUT_ENCRYPTION=0` deployment override disables the output-at-rest guarantee.
- Verification: complete Studio suite passes 180/180; Media Gateway suite passes 41/41; ComfyUI Mobile passes 937/937; OpenGen regressions pass 23/23; ComfyUI Mobile and OpenGen production bundles build successfully.
- Intended commit message: `fix: harden studio creative privacy`

## 2026-07-16 14:24:13 -0400 — Direct Media Studio renders encrypt at rest

- Status: Uncommitted
- User-facing result: Local video generations launched from the embedded OpenGen Studio now keep the uploaded start frame temporary and remove it after the Media Studio MCP call. The returned MP4 is immediately converted into an authenticated `.zenc` sidecar, the plaintext MP4 is removed, playback still works through `/api/media-studio/generated/{filename}` with range support, and local workflow prompts are not persisted in the embedded browser history.
- Areas changed: Content Studio private byte encryption helper; direct `/api/media-studio/video` output handling; authenticated generated-media playback; OpenGen local-workflow history prompt redaction; API/UI regression coverage; rebuilt served OpenGen dist bundle.
- Security boundary: the browser receives the same owner/control-gated private media URL, a safe output filename, sanitized QA metadata, and an `encrypted_at_rest` flag. It no longer receives local output paths from the MCP result, QA paths, prompt echoes, or persistent plaintext reference-image files. Hivemind/Media Studio prompt text is sent only for generation and then redacted from OpenGen `video_history`; existing local workflow history entries are scrubbed on load.
- Verification: focused encrypted Media Studio endpoint and local-workflow selector/history regressions pass; broader Studio API/UI contract suite passes 47/47; `python3 -m py_compile src/hivemind_content_studio/control_api.py src/hivemind_content_studio/private_access.py` and `node --check packages/open-generative-ai/src/components/VideoStudio.js` pass; `npm run vite:build` rebuilt the served OpenGen bundle; managed stack restarted and live health reports `{"ok":true}` on `127.0.0.1:8765`; live catalog still reports the Media Studio MCP LTX workflow list; LTX queue is empty after restart.
- Intended commit message: `fix: encrypt direct media studio outputs`

## 2026-07-16 14:08:07 -0400 — Local LTX workflows in OpenGen Video Studio selector

- Status: Uncommitted
- User-facing result: The embedded Studio video model selector now shows local Hivemind/Media Studio workflows such as `LTX 2.3 Eros Fast`, `LTX 2.3 Eros Exact`, and the registered LTX 2.3 LoRA workflows directly in the normal Video Studio model dropdown instead of hiding them behind image mode or the Hivemind side dock. Selecting one switches the generation path to the same-origin Media Studio MCP route.
- Areas changed: OpenGen Video Studio model list composition and Hivemind workflow selection behavior; UI contract coverage; rebuilt served OpenGen dist bundle.
- Security boundary: local workflows still use same-origin Content Studio owner/control auth and the existing `/api/media-studio/video` bridge. No provider keys, MCP tokens, private workflow JSON, or local filesystem paths are exposed to the browser.
- Verification: live catalog probe with shared env confirms `media-studio-mcp` is available and returns `workflow-default`, `ltx23-eros-fast`, `ltx23-eros-exact`, `ltx23-regular-fp8`, `ltx23-transition-lora`, `ltx23-better-motion-lora`, and `ltx23-ic-dual-character-lora`; focused OpenGen/Studio UI contract passes; complete `test/studio/test_studio_ui_contract.py` passes 18/18; `node --check` passes for the touched OpenGen modules; `npm run vite:build` rebuilt the served OpenGen bundle; dist grep confirms the served bundle includes the Hivemind local workflow marker; `git diff --check` passes.
- Intended commit message: `fix: show local ltx workflows in video studio`

## 2026-07-16 — Regular-LTX renders survive frontend stalls and receive negative conditioning

- Status: Uncommitted
- User-facing result: Long regular-LTX renders are no longer terminated when accelerator saturation temporarily stalls the Studio frontend, and `media_generate_video` now passes a caller-provided negative prompt into registered video workflows.
- Areas changed: managed-stack frontend watchdog policy; Media Studio MCP video input contract; focused source-contract regressions.
- Safety boundary: exited child processes remain fatal, only the already-designated soft frontend HTTP check is non-fatal, bearer authentication is unchanged, and prompts remain excluded from stored history.
- Verification: reproduced the six-timeout supervisor restart during an active LTX job; the corrected stack completed subsequent regular-LTX renders without restart; local Studio suite passes 149/149; live M5 MCP syntax, negative-prompt schema, listener, and final regular-LTX generation checks pass.
- Intended commit message: `fix: preserve and condition regular ltx renders`

## 2026-07-15 18:22:19 -0400 — Explore becomes the core Studio surface

- Status: Uncommitted
- User-facing result: The primary Studio nav and no-hash shell route now open the Explore/OpenGen generation UX. The former native composer remains available as Planner for agent-directed production, history restore, templates, ingredients, prompt-helper/walk-through options, route pickers, and durable run workflows.
- Areas changed: Content Studio shell routing and Explore iframe boot params; parent-to-Explore prompt/template/ingredient bridge; OpenGen Hivemind dock; dynamic discovery of `media-studio-mcp` video workflows; OpenGen Video Studio Hivemind image-to-video model routing; same-origin Media Studio video endpoint accepting inline start images and returning private MP4 URLs; focused API/UI contracts.
- Security boundary: local workflow generation stays owner-session protected and same-origin. The browser sends an inline start image to `/api/media-studio/video`; the server writes it to private temp storage, calls the existing Media Studio MCP adapter, downloads output under the private generated-media root, and serves only a checked filename URL. Wan2GP/Electron local-AI routing remains separate and unchanged.
- Verification: focused contracts pass: `.venv/bin/pytest test/studio/test_studio_ui_contract.py test/studio/test_control_api.py`; Media Studio/provider contracts pass: `.venv/bin/pytest test/studio/test_media_studio.py test/studio/test_provider_execution.py`; JavaScript syntax checks pass via `node --check`; Python compile checks pass via `python3 -m py_compile src/hivemind_content_studio/control_api.py src/hivemind_content_studio/media_studio.py`; OpenGen production bundle builds with `npm run vite:build`; TestClient smoke confirms `/` and `/open-gen/` serve 200 after owner unlock.
- Intended commit message: `feat: make explore the core studio surface`

## 2026-07-15 17:29:19 -0400 — Owner run controls in Runs view

- Status: Uncommitted
- User-facing result: The Runs view no longer blocks Resume, Retry step, or Cancel behind the Advanced operator-token field when the browser already has an unlocked owner session. Operator bearer tokens still work for machine/non-owner clients, and locked browsers without a token remain rejected.
- Areas changed: Content Studio run-control API authorization; Runs view run-action frontend guard; focused API/UI regression coverage.
- Security boundary: approval decisions remain operator-token gated. This change only lets the existing owner session cookie authorize local run lifecycle controls that were already visible in the owner UI.
- Verification: focused regression checks pass: `uv run pytest test/studio/test_control_api.py::test_control_api_is_a_thin_run_viewer_with_owner_or_operator_mutations test/studio/test_studio_ui_contract.py::test_ui_is_same_origin_and_uses_session_storage_only_for_tab_scoped_owner_handoff`; broader API/UI contracts pass: `uv run pytest test/studio/test_control_api.py test/studio/test_studio_ui_contract.py`; `python3 -m py_compile src/hivemind_content_studio/control_api.py`, `node --check src/hivemind_content_studio/ui/studio.js`, and `git diff --check` pass.
- Intended commit message: `fix: allow owner sessions to use run controls`

## 2026-07-15 21:08:00 -0400 — LTX 2.3 Eros video workflow discovery

- Status: Uncommitted
- User-facing result: The Video model picker can now discover the managed Media Studio MCP route even when no HivemindOS `mcpVideo` app preference is present, and it exposes the built-in LTX 2.3 Eros workflow choices (`ltx23-eros-fast`, `ltx23-eros-exact`) instead of only a generic workflow-default option. Selecting `ltx23-eros-fast` preserves that choice into the run and dispatches it as the Media Studio `workflow_id`, which uses the optimized LTX Eros route.
- Areas changed: managed Media Studio MCP fallback discovery and token-file auth; workflow listing from Media Studio MCP; Simple composer media catalog entries for LTX Eros workflows; executor routing from nested `motion.model` to Media Studio `workflow_id`; run restore reads nested media model selections; focused discovery/catalog/execution tests.
- Security boundary: no secrets are printed or sent to the browser. Token-file reading is process-local and only used to authenticate the managed MCP. The browser receives safe workflow ids, labels, readiness, and reference-image limits only; generation still goes through the existing MCP, run manifest, and provider intent boundaries.
- Verification: reproduced the baseline catalog status showing `media-studio-mcp` unavailable with only `workflow-default`; with the shared env loaded, live post-change status reports managed MCP reachable and `media_list_workflows` returns `ltx23-eros-fast` as default plus `ltx23-eros-exact`. The composer catalog lists `workflow-default`, `ltx23-eros-fast`, and `ltx23-eros-exact`. Focused tests pass: `uv run pytest test/studio/test_media_studio.py test/studio/test_control_api.py::test_simple_catalog_combines_safe_hivemind_brains_and_media_capabilities test/studio/test_provider_execution.py::test_media_studio_motion_uses_the_selected_workflow_model`. Python compile and `node --check src/hivemind_content_studio/ui/studio.js` pass.
- Intended commit message: `fix: discover LTX Eros video workflows in composer`

## 2026-07-12 02:22:15 +0800 — Run history restores reference images

- Status: Uncommitted
- User-facing result: Loading a previous run from History now restores its ordered reference images alongside the original prompt, brain, provider/model routes, and production toggles. Restored images render immediately in the composer, remain available to the vision-capable planning brain, and carry into the next production without the browser downloading and re-uploading the full-resolution files.
- Areas changed: reference-artifact restoration and attachment rendering in the Simple composer; planning-brain image hydration from same-origin artifact routes; prior-run artifact references in Simple-run creation; secure server-side reference-image copying; focused API and UI regression coverage.
- Security boundary: the browser submits only prior run/artifact identifiers. The backend resolves those identifiers against the canonical run manifest, accepts only image artifacts with `reference-*` roles, requires the file to remain inside that run's directory, reapplies the 30-image, 50 MB per-image, 500 MB total, decode, and immutable-ingestion checks, and never accepts a client-supplied filesystem path.
- Verification: strict TDD baseline 3/3 failing for missing restore/reuse/rejection behavior → focused contracts green; complete `test/studio` suite passes 135/135; Python compileall, JavaScript syntax, and the 1,500-line source limit pass. Live browser QA loaded the 02:05 “Level Up Your Confidence IRL” History entry and confirmed its original prompt/model plus one loaded 1320px PNG at the scoped artifact route with a `1/30` attachment badge. The latest run correctly restores no image because that specific run has no reference artifact. No planning, generation, upload, or spend was triggered.
- Intended commit message: `fix: restore reference images from run history`

## 2026-07-12 02:15:03 +0800 — HivemindOS image and video generation cards

- Status: Uncommitted
- User-facing result: Simple productions now render the same interaction model as HivemindOS chat generation: separate Image generation and Video generation cards instead of a generic “video production” run summary. Each card shows the real provider/model, honest waiting/generating/ready/error state, a 16:9 generation surface, telemetry-backed elapsed/progress when an attempt is active, source-image thumbnail and preview, generated image gallery or video playback, prompt context, and a direct link to the corresponding durable run step. The newest run’s cards also restore after reload and update from polled run artifacts/events.
- Areas changed: HivemindOS `ApplicationGenerationCard` schema/adapter behavior and `ImageGenerationCard.module.css` were traced as the reference; Content Studio gained run-to-image/video card adapters, generation-attempt state mapping, persisted artifact/source URLs, telemetry progress, image preview modal, refresh/timer lifecycle, and matching responsive card styles; focused UI contract coverage.
- Security boundary: card URLs remain manifest-scoped artifact endpoints validated by the existing API. The UI does not receive filesystem paths, credentials, OAuth tokens, or provider payloads. Waiting is distinct from generating, so the card does not fabricate progress before an actual generation attempt exists; paid generation, approval, budget, evaluation, and publishing gates remain unchanged.
- Verification: strict TDD reproduced the missing Hivemind card contract; complete `test/studio` suite passes 132/132; Python compileall, JavaScript syntax, 1,500-line file-size limits, and diff checks pass. Live browser QA against the existing `Level Up Your Confidence IRL` run confirms two cards (OpenAI GPT Image OAuth / `gpt-image-2`, then MUAPI / `seedance-v2.0-t2v`), correct waiting dependencies, restored original prompt, real source thumbnail and working preview modal, local API readiness, and zero browser warnings/errors. No generation or spend was triggered.
- Intended commit message: `fix: use HivemindOS generation cards in Simple mode`

## 2026-07-12 01:55:34 +0800 — Privacy-safe generation telemetry

- Status: Uncommitted
- User-facing result: Every run-associated image, video, voice, and music generation now records success or failure, provider/model, duration, artifact count, charged amount, and safe error class—including deterministic stickman/static frames produced automatically during run resume. The studio gains a compact Telemetry view with fleet-style summary cards, provider reliability, timing, spend, and recent attempts. Agents receive the same evidence through API, CLI, MCP tool, and MCP resource, while completed timing samples feed HivemindOS's existing cross-app generation metrics best-effort.
- Areas changed: run event querying; shared generation-attempt recorder and HivemindOS timing bridge; canonical intent instrumentation; deterministic orchestrator generation instrumentation; `/api/telemetry/generations`; `content-studio telemetry generations`; `get_generation_telemetry` and `studio://telemetry/generations`; responsive Telemetry UI; agent skill and product/architecture/operations documentation; focused execution, failure, aggregation, API, CLI, MCP, UI, and automatic-local-generation contracts.
- Security boundary: telemetry is local and allowlist-shaped. It excludes prompts, reference images, generated media, credentials, OAuth tokens, provider payloads, raw error messages, and private URLs. Mirroring sends only media kind, Content Studio/provider identity, model id, duration, and an opaque run-attempt id to the authenticated local HivemindOS metrics route. Telemetry does not authorize generation, spending, retries, or publishing.
- Verification: strict TDD baseline failed at the missing module/constructor/surfaces; focused telemetry contracts pass, including exact success/failure event shapes and the authenticated HivemindOS bridge allowlist; complete `test/studio` suite passes 131/131; Python compileall, JavaScript syntax, MCP server construction, 1,500-line file-size limits, and diff checks pass. The restarted live API returns the privacy declaration and zero-valued aggregate schema against the existing database; live browser QA confirms the Telemetry route, four summary cards, provider/recent empty states, responsive navigation contract, local API readiness, and zero console warnings/errors. No generation or spend was triggered for verification.
- Intended commit message: `feat: add privacy-safe generation telemetry`

## 2026-07-12 01:36:36 +0800 — Run settings restore and Simple-mode script handoff

- Status: Uncommitted
- User-facing result: The Simple composer now restores the newest run’s original prompt, planning brain, image/video provider routes, Prompt Helper choice, and Walk-through choice after reload. Every run exposes “Use prompt & settings,” and Prompt History reuses the associated run configuration when available. Simple productions no longer stop immediately at `Attach_script`: the approved brain draft becomes a durable runtime-neutral `script.md` automatically and the run advances to the next real generation, evaluation, or approval boundary.
- Areas changed: per-run composer and original-prompt snapshot in the canonical manifest and run envelope; backwards-compatible prompt-history composer migration; deterministic provider-neutral script renderer; Simple-run script attachment/resume lifecycle; newest-run boot restoration and run/history composer actions; focused API and UI contracts.
- Security boundary: browser storage is not used. Snapshots contain only provider/model identifiers, auth route labels, booleans, and prompt text—never API keys or OAuth tokens. Automatic script attachment is limited to the already-approved Simple brain draft; it does not execute a paid media provider, bypass spend/rights/evaluation/publish gates, or auto-attach scripts to Advanced/external-agent runs.
- Verification: strict TDD baseline 4/4 failing for the missing composer/script/UI contracts → focused contracts green; complete `test/studio` suite passes 125/125; JavaScript syntax check passes. The user’s stuck `20260711T172709975875Z-level-up-your-confidence-irl` run was recovered from its validated brief: script is completed with a script artifact and the run now waits at `keyframes` / `generate_keyframes`, before any paid generation dispatch.
- Intended commit message: `fix: restore run settings and auto-attach simple scripts`

## 2026-07-12 01:14:16 +0800 — Brain provider options normalize into executable provider configuration

- Status: Uncommitted
- User-facing result: Production creation no longer fails when a planning brain emits role-oriented provider choices such as `image_model`, `motion_model`, or a scalar reference-usage note. Model choices are mapped into the selected provider's canonical `{model: ...}` options, while other planning metadata is retained safely under `_planning`.
- Areas changed: `StudioRunDraft` pre-validation normalization in `studio_drafts.py`; exact-shape regression in `test_studio_drafts.py` using the reported GPT Image 2, Seedance 2.0, and reference-image fields.
- Security boundary: normalization only reshapes already-returned planning metadata. It does not execute a provider, approve spending, change credentials, bypass run validation, or weaken provider/role validation.
- Verification: strict TDD RED reproduced the same three Pydantic `dict_type` errors; GREEN focused regression passes; complete `test/studio` suite passes 123/123; Python compileall, lock, and diff checks pass. The restarted live API received the same malformed provider-options shape and advanced past all three fields, stopping only at a deliberately injected unrelated missing-source error; no run or provider action was created during that boundary proof.
- Intended commit message: `fix: normalize role-oriented brain provider options`

## 2026-07-12 00:29:59 +0800 — Searchable HivemindOS provider and model pickers

- Status: Uncommitted
- User-facing result: The Simple composer now uses the HivemindOS popover picker pattern for Brain, Image, and Video instead of browser-native selects. Each picker has token-aware provider/model search; clearly separated API Key, OAuth, and Local & Managed routes; collapsed provider rows with ready/unavailable state; selected-route and auth badges; disabled-model explanations; and an Automatic route for image/video. The same picker remains usable on narrow screens with an internally scrollable, viewport-bounded panel.
- Areas changed: Simple composer picker markup in `index.html`; shared picker taxonomy, filtering, expansion, selection, keyboard dismissal, focus restoration, and responsive height behavior in `studio.js`; HivemindOS-style pill, popover, auth-section, provider, model, status, selected, disabled, desktop, and mobile styling in `studio.css`; focused UI contract coverage.
- Security boundary: API keys, OAuth tokens, and credential values remain server-side. The browser receives and displays only provider/model catalog metadata, readiness, auth-route labels, capability limits, and safe unavailable reasons; choosing API Key versus OAuth selects an existing backend route and never copies a credential into the frontend.
- Verification: focused UI/API baseline 26/26 → final 27/27; complete `test/studio` suite 122/122; JavaScript syntax and the 1,500-line file-size gate pass. Live browser QA against `http://127.0.0.1:8765/#create` confirmed all three custom triggers, 11 image providers split into 4 API Key / 2 OAuth / 5 Local & Managed groups, collapsed-provider expansion, token-aware `OpenAI OAuth` filtering, successful selection of OpenAI OAuth GPT Image 2 with the trigger changing to an OAuth badge, Escape dismissal with trigger focus restoration, and zero paid generation. Visual QA at the default viewport and 390×844 confirmed the popover stays inside the usable viewport and scrolls its provider list without obscuring the composer controls.
- Intended commit message: `feat: add searchable auth-grouped provider pickers`

## 2026-07-11 23:36 PST — Brain picker badges vision models and auto-prefers one when images are attached

- Status: Uncommitted
- User-facing result: Brain models that can actually see attached reference images now show a "· vision" suffix in the Brain selector (e.g. "google/gemini-2.5-flash-lite · vision · recommended"). Attaching an image while a known text-only brain is selected automatically switches to the best available vision brain with an explanatory toast; deliberately re-picking a text-only brain with images attached is respected but warns immediately ("… is text-only — it can't see your attached images") instead of waiting for the post-plan degradation notice. Models whose modality is unknown are left alone — the existing graceful text-only fallback still covers them.
- Areas changed: `src/hivemind_content_studio/ui/studio.js` only (`modelOptionLabel` vision suffix; new `selectedBrainModel`/`preferVisionBrain` helpers; attach-path hook in `addSimpleImages`; `#simple-brain` change warning). The tri-state `vision` flag arrives through the existing `/api/simple/catalog` pass-through from the companion HivemindOS change (provider model discovery now parses OpenRouter `input_modalities` and Venice `supportsVision`); no Python changed.
- Security boundary: unchanged — the browser still receives only model names, readiness, and now a boolean capability flag; keys and tokens stay server-side.
- Verification: studio + Auto Clipper suite 134/134 (baseline 134/134, no Python touched); `node --check` passes. Live browser QA on a fresh instance (port 8767; 8766 was occupied by another session) against live HivemindOS: 218 of 1,070 brain options render the vision badge; selecting text-only `deepseek/deepseek-v4-flash` and attaching a PNG through the real file input auto-switched the selector to `openai/gpt-5-nano · vision · recommended` with the switch toast; re-picking deepseek with the image still attached kept the selection and raised the error toast; zero console errors. `.claude/launch.json` test port bumped 8766 → 8767 (next free port; no running process touched).
- Intended commit message: `feat: badge vision-capable brains and auto-prefer one when reference images are attached`

## 2026-07-11 23:14:29 +0800 — Production template catalog

- Status: Uncommitted
- User-facing result: The studio now ships a typed production-template catalog. The Simple composer gains a Templates menu (grouped: UGC realism, Winning formats, Animation) that inserts a complete composer-ready prompt with [SLOT] placeholders for the brain to expand. Eleven templates ship: the hyper-real AI UGC character-reference and 8-beat 15-second product-ad system (named imperfections, character/outfit/environment/product/voice locks, full negative stack), eight proven viral ad formats as runnable beat plans, and a brand-explainer arc built on the existing animation scaffolds. Agents get the same catalog through `GET /api/templates` and `content-studio templates [template-id]`.
- Areas changed: new `template_catalog.py` loader over frontmatter-markdown files in `templates/catalog/<category>/`; `/api/templates` route and `templates` key in `/api/simple/catalog`; `templates` CLI subcommand; Simple composer Templates menu (index.html, studio.js, studio.css); template/API/CLI tests; README, THIRD_PARTY_NOTICES.
- Security boundary: templates are static packaged prompt text plus safe metadata (lane hint, aspect ratio, duration, tags, provenance); no secrets, provider credentials, or remote fetches. Inserting a template only fills the composer textarea; planning, budget, approval, and publishing gates are unchanged.
- Verification: baseline studio + Auto Clipper suite 130/130 → final 134/134 (4 new tests, 0 regressions). JavaScript syntax (node --check) and Python compileall pass. Live browser QA on the local API: the Templates menu renders 3 groups and 11 entries, clicking "UGC product ad — 8-beat 15s" inserts the full 3,292-character prompt into the composer and focuses it, `/api/templates` and `/api/simple/catalog` both serve 11 templates over HTTP, and the console shows zero errors. UGC and format template bodies are adapted from the publicly distributed UGC Lab prompt system (ugc-lab.twoclipping.workers.dev) with provenance recorded per-template and in THIRD_PARTY_NOTICES.
- Intended commit message: `feat: add production template catalog with UGC realism and winning-format templates`

## 2026-07-11 23:10:00 +0800 — Prompt history, ingredients, drag-and-drop, AVIF, auto-scroll, and a brain that reads reference images

- Status: Uncommitted
- User-facing result: Every production now records the prompt that ultimately drove generation (the brain-expanded concept, or your exact words) in a new History view, where entries can be favorited, reused, or deleted. A new Ingredients menu in the chat composer drops any favorited prompt straight into the prompt box. Images can be dragged and dropped onto the composer, and modern formats (AVIF/HEIC) now upload, validate, and preview correctly. The thread auto-scrolls to new messages and production cards. Most importantly, attached reference images are now actually delivered to the planning brain: vision-capable models read them (verified: a checklist screenshot's exact task wording appears in the plan), and text-only models now say plainly that they cannot see images instead of silently inventing content or failing with HTTP 404. Brain drafts that contain null fields or invented platform names ("instagram_reels") no longer kill run creation.
- Areas changed: durable prompt-history store and `/api/simple/prompts` endpoints; run-creation prompt recording; composer attachment payloads (client-downscaled JPEG data URLs for the brain); drag-and-drop and `image/*` intake with extension fallback; Pillow AVIF/HEIF decode plugins for asset QA; lenient brain-draft validation (null tolerance, publish-platform normalization); History view, Ingredients menu, deterministic thread auto-scroll; UI contract, control API, and draft tests. Companion HivemindOS change delivers image content parts to OpenAI-compatible/Anthropic brain transports.
- Security boundary: prompt history is stored server-side next to the run store (no browser storage); image bytes flow only through the existing authenticated HivemindOS planning proxy; unknown publish platforms are dropped rather than routed.
- Verification: studio + Auto Clipper baseline 109/109 before changes; after, 117/117 including 8 new tests (history/favorites lifecycle, post-edit prompt recording, AVIF ingest through the API, attachment data passthrough, platform normalization, null tolerance, UI contract). Browser QA on a fresh instance (port 8766) against live HivemindOS: drag-drop attach with highlight, AVIF drop preview + decode, DeepSeek v4 flash (text-only) degrades with an honest warning, Gemini 2.5 Flash Lite reads the checklist and quotes "Initiate a Conversation With a Stranger" verbatim in the produced brief, run creation succeeds, History records and favorites the prompt, Ingredients inserts it back into the composer, auto-scroll keeps the thread pinned to the newest message.
- Intended commit message: `feat: prompt history with favorites, composer ingredients, image drag-and-drop, AVIF support, auto-scroll, and vision-grounded planning`

## 2026-07-11 20:57:35 +0800 — Agent-directed Simple creation mode

- Status: Uncommitted
- User-facing result: Create now opens as a focused HivemindOS-style prompt composer. Users can select any locally detected planning model, leave image/video providers on Automatic or pin a provider/model, attach up to 30 ordered reference images, keep Prompt Helper enabled, and turn on Walk-through to gather requirements and require confirmation. The complete former studio remains available unchanged under Advanced. Plans and references land in the same durable canonical run, with HivemindOS-style image/video production cards for progress and handoff.
- Areas changed: Simple/Advanced creation shell; chat composer and generated-media card UI; authenticated HivemindOS brain catalog/planning proxy; typed image/video provider-model capability matrix; multi-image upload/validation/provenance; studio draft provider options; responsive styling; API, asset, UI, and capability tests; README.
- Security boundary: browser code receives model names, readiness, capability roles, and limits only. API keys and OAuth tokens stay in HivemindOS/shared server environment. The 30-image figure is a studio intake ceiling, not a fabricated Seedance limit; models backed by dynamic MUAPI/Higgsfield/MCP schemas are marked for live-schema validation at dispatch. Planning does not bypass generation spend, approval, privacy, rights, evaluation, or publishing gates.
- Verification: focused studio baseline 27/27; final focused API/UI/asset/provider suite 33/33; broader Studio + Auto Clipper suite 122/122. JavaScript syntax, Python compileall, lock, dependency, diff, and HivemindOS focused lint pass. The authenticated live catalog proxy returns 6 configured provider groups and 1,070 detected model entries; 564 unsupported gateway/non-chat entries remain visible but disabled for planning. Browser QA confirms Simple is the default, Advanced still exposes all seven lanes and provider controls, Prompt Helper defaults on, Walk-through defaults off, selectors expose 1,070 brain, 27 image, and 20 video choices including Automatic, the local API reports ready, and no console errors appear. No paid planning or generation request was submitted.
- Intended commit message: `feat: add agent-directed simple content creation`

## 2026-07-11 19:45:12 +0800 — GPT Image and xAI Imagine providers

- Status: Uncommitted
- User-facing result: OpenAI GPT Image now appears as two independent image/keyframe providers: the official API-key route and a ChatGPT/Codex OAuth route using the Responses `image_generation` tool. xAI Imagine likewise appears as separate API-key and HivemindOS OAuth providers for images, image-to-video, and video. The Providers view exposes safe OpenAI/xAI connect and reconnect controls.
- Areas changed: provider capability matrix and labels; official GPT Image executor; Codex OAuth GPT Image executor; xAI Imagine image/video executor with asynchronous polling; HivemindOS OAuth status/start proxies; HivemindOS OpenAI and xAI OAuth media bridges; manifest executor registration; frontend connected-account cards; environment contract; architecture and operations docs; provider, generation, execution, API, and UI tests.
- Security boundary: OpenAI OAuth is used only against the ChatGPT/Codex Responses backend and is never presented to the public Image API. OpenAI and xAI OAuth tokens remain in HivemindOS and never enter the content-studio process or browser. Direct and OAuth generation remain paid/subscription-backed providers behind exact-scope approval, bounded cost, privacy, artifact, and provenance gates. No live paid generation was called.
- Verification: focused content provider/generation/execution/API/UI tests pass 39/39 (baseline 38/38); studio + Auto Clipper passes 118/118; the full suite excluding the same two known external voice checks passes 150/150 with 1 skipped. The HivemindOS OpenAI and xAI OAuth media-bridge contracts, chat title/stream regressions, focused zero-warning lint, and full TypeScript pass. Python compileall, JavaScript syntax, lock, dependency, diff, live authenticated route, and file-size checks pass. Live status confirms the shared API key and OpenAI OAuth providers are independently ready; xAI OAuth still needs reconnect. Browser QA confirms both OpenAI providers visible and ready, the OAuth card connected with accurate copy, and zero console errors. No live paid image generation was called.
- Intended commit message: `feat: add GPT Image and xAI Imagine providers`

## 2026-07-11 18:59:46 +0800 — Secure shared Hive provider discovery

- Status: Uncommitted
- User-facing result: provider credentials already stored in the shared HivemindOS environment now make the studio's provider routes ready automatically, without re-entering variables in the browser or wrapping the server command manually.
- Areas changed: canonical shared-env parser/loader; process-first shared fallback at the studio server/config boundary; DRY reuse by the MoneyPrinter compatibility config; provider catalog security tests; README and operations guidance.
- Security boundary: secrets remain server-side in process memory. The frontend receives only readiness booleans, capability metadata, and safe status text. Explicit process/project values override shared defaults, and protected operator credentials are not sent to or autofilled in the browser.
- Verification: focused shared-env, control API, and provider tests pass 17/17; studio + Auto Clipper passes 109/109; the full suite excluding the same two known external voice checks passes 141/141 with 1 skipped. Synthetic secret values are absent from the complete catalog response. The restarted live server reports MUAPI, Higgsfield Cloud, ElevenLabs, Upload-Post, Pexels, and Pixabay ready from existing shared configuration, while Postiz correctly remains unavailable because its required shared key is absent. Python compileall, lock, dependency, diff, and live HTTP checks pass.
- Intended commit message: `feat: load provider credentials from shared Hive env`

## 2026-07-11 18:47:34 +0800 — Unified creation and operations studio

- Status: Uncommitted
- User-facing result: creators and agents now have one complete browser studio for starting animation ads, stickman ads, static creatives, general animation, faceless videos, long-form clips, and social publishing; the default flow stays simple while provider routing, voice/captions, distribution, MoneyPrinter-derived faceless settings, privacy, budget, and operator controls remain collapsed until needed.
- Areas changed: typed production-lane catalog; validated browser draft contract; safe same-origin run creation and manifest-scoped artifact routes; responsive Create, Runs, and Providers views; scene editor; provider readiness/routing; durable workflow inspection; compatibility documentation; UI and API contract tests. The obsolete inline run-monitor shell was removed.
- Verification: focused API/UI suite passes 13/13 and studio + Auto Clipper passes 107/107; a real browser loaded all seven lanes and provider choices without console errors, expanded advanced provider routing, and created a durable `awaiting_agent` run with workflow steps and 11 manifest artifacts. The full inherited suite reaches 139 passed/1 skipped with only the same two external voice failures (Azure WebSocket 401 and missing SiliconFlow key). Python compileall, JavaScript syntax, lock, dependency, diff, and file-size checks pass. The local server is live at `http://127.0.0.1:8765` with the Create view open.
- Intended commit message: `feat: add unified content creation studio`

## 2026-07-11 17:45:12 +0800 — Audited Clueso agent video workflow shelf

- Status: Uncommitted
- User-facing result: general agents can discover and apply all 90 Clueso motion-graphics, video creation, editing, localization, repurposing, and documentation workflows from this project while Hivemind Content Studio remains the single provider-routing, privacy, cost, approval, rights, and publishing authority.
- Areas changed: pinned byte-for-byte Clueso upstream snapshot; namespaced DRY agent adapters and canonical studio agent link; central provider policy; source and npm-installer audit/provenance; reproducible no-telemetry vendor script; provider capability matrix; agent skill, architecture, operations, README, licensing, and contract tests.
- Security boundary: no network installer ran in the project. The source is pinned by commit, archive hash, and 90 per-skill hashes; the telemetry-enabled installer was audited and sandboxed only. Clueso is identified as an agent-scoped remote SaaS boundary and remains unavailable until the acting runtime verifies authenticated MCP tools. Vendor instructions cannot silently select Clueso, configure a runtime, upload media, spend, publish, or bypass studio governance.
- Verification: pinned source validator passes 90/90; all 90 namespaced adapters plus the canonical studio skill pass `quick_validate.py`; vendor output reproduced byte-for-byte in a disposable repository; bundle/provider tests pass 9/9 (baseline 8/8 relevant existing tests); studio + Auto Clipper pass 96/96; full inherited suite is 128 passed/1 skipped with only the same two external live-voice failures (Azure WebSocket 401 and missing SiliconFlow key), a +4 passing-test delta from the prior 124-pass baseline; compileall, lock, dependency, diff, file-size, live provider-report, symlink, and final per-skill hash checks pass; npm production audit reported zero known vulnerabilities; the installer sandbox copied 90/90 byte-identical files and wrote nothing outside its disposable project.
- Intended commit message: `feat: add audited Clueso video workflow skill shelf`

## 2026-07-11 17:03:11 +0800 — Zero-config HivemindOS hosted media credits

- Status: Uncommitted
- User-facing result: content agents can now quote and generate keyframes or scene motion through HivemindOS hosted credits without configuring a media-provider key; the private gateway charges provider cost plus 25%, while existing local, fleet, MUAPI BYOK, and Higgsfield paths remain available.
- Areas changed: hosted-media provider capability; authenticated local HivemindOS adapter; quote/max-debit/idempotency binding; async polling and artifact provenance; provider execution context; environment contract; agent skill, README, architecture, and operations docs.
- Security boundary: the studio never receives the hosted provider key or credit token; HivemindOS remains authoritative for company freeze, budgets, approval thresholds, balance, reservation, refund, reconciliation, and receipts. Direct paid providers still require the studio's exact one-time approval receipt.
- Verification: hosted adapter/provider/provider-execution/intent tests pass 17/17; studio + Auto Clipper pass 92/92; compileall, lock, and dependency checks pass. The full inherited suite is 124 passed/1 skipped with only the same two external live-voice failures (Azure WebSocket 401 and missing SiliconFlow key). The deployed hosted catalog exposes 401 media models at 25% markup; the authenticated local route returns a $0.01875 retail quote for a $0.015 Flux request; a below-quote submit fails before provider spend and leaves the hosted balance unchanged. Runtime token discovery and the configured Creative Producer company identity report the hosted provider ready without a provider key.
- Intended commit message: `feat: add HivemindOS hosted media credit provider`

## 2026-07-11 16:30:56 +0800 — Agent-first durable content company control plane

- Status: Uncommitted
- User-facing result: agents can now create and resume durable production runs; select local, fleet, or cloud providers by intent; generate first-frame, stickman, and static text ads; attach safe remote assets; evaluate content; request operator approvals; publish through Postiz or Upload-Post; and learn from retention, spend, conversion, and revenue evidence through one unified repository.
- Areas changed: manifest schema v2 and optimistic cross-process writes; SQLite run/step/event/budget store; capability router and registered intent executors; registered agent runtime ids; secure asset ingestion and provider downloads; exact-scope one-time approval ledger; MUAPI/Higgsfield/Media Studio/Universal TTS execution; static text rendering; semantic evaluation; experiment lineage and performance metrics; agent-first MCP resources/tools; authenticated FastAPI operator console; CLI lifecycle/approval commands; examples, environment contract, architecture/operations docs, repo/Shared Brain skills, and the existing Hivemind Content Studio company directive.
- Security boundary: paid execution records spend only after success; MCP can request but cannot decide approval; run approval consumes a separate receipt; arbitrary agent argv, private download URLs, unbounded downloads, guessed MUAPI schemas, and silent Higgsfield surface switching are rejected.
- Verification: 87 focused studio + Auto Clipper tests passed; full inherited suite reached 119 passed/1 skipped with only the same two donor live-voice failures (Azure WebSocket 401 and missing SiliconFlow key); a real MCP-path static-ad run rendered one keyframe, persisted $0 spend, and stopped correctly at semantic evaluation; compileall, `uv pip check`, `uv lock --check`, and `git diff --check` passed; the repo skill, Shared Brain studio skill, and mandatory HivemindOS `create-zero-human-company` packaged skill passed `quick_validate.py`; the company directive was read back by exact ID with status still `setup` and autonomy off; all three approval/control key names report present in the shared env. Live paid generation and live social publishing were intentionally not called.
- Intended commit message: `feat: add durable agent-first content studio control plane`

## 2026-07-11 15:36:43 +0800 — General-agent ad production lanes

- Status: Uncommitted
- Areas changed: canonical provider matrix; general agent script runtime; first-frame animation and stickman ad plans; deterministic stickman renderer and FFmpeg assembly; portable CapCut handoff; MUAPI, Higgsfield Cloud/consumer, and ElevenLabs adapters; MCP/CLI surfaces; shared skill snapshots; examples and operating docs.
- Verification: 46 studio + Auto Clipper tests passed; real CLI plan/render/assemble/CapCut-handoff smoke passed with a 75,436-byte vertical MP4; full inherited suite reached 78 passed/1 skipped with the same two donor live-voice failures (Azure 401 and missing SiliconFlow key); live paid generation was intentionally skipped.
- Intended commit message: `feat: add general-agent animation and stickman ad pipelines`
