// Single source of truth is the vendored model data in ./modelsData.js
// (auto-generated from models_dump.json). This file exists only so the
// standalone (Electron/Vite) build's existing imports of "../lib/models"
// keep resolving without touching every consumer.
export * from './modelsData.js';
